import { assertFoundationChapterDefinition } from './schema.js'

const source = (id, label, pages, href) => ({ id, label, pages, href })

const sources = {
  dpo: [source('dpo', 'Direct Preference Optimization: Your Language Model is Secretly a Reward Model', 'Sections 3–5', 'https://arxiv.org/abs/2305.18290')],
  grpo: [
    source('grpo', 'DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models', 'Section 4', 'https://arxiv.org/abs/2402.03300'),
    source('dapo', 'DAPO: An Open-Source LLM Reinforcement Learning System at Scale', 'Sections 2–3', 'https://arxiv.org/abs/2503.14476'),
  ],
  coding: [
    source('rlef', 'RLEF: Grounding Code LLMs in Execution Feedback with Reinforcement Learning', 'Sections 2–4', 'https://arxiv.org/abs/2410.02089'),
    source('swerl', 'SWE-RL: Advancing LLM Reasoning via Reinforcement Learning on Open Software Evolution', 'Sections 2–4', 'https://arxiv.org/abs/2502.18449'),
  ],
  agent: [source('agent-lightning', 'Agent Lightning: Train ANY AI Agents with Reinforcement Learning', 'Sections 2–4', 'https://arxiv.org/abs/2508.03680')],
  credit: [
    source('agent-lightning', 'Agent Lightning: Train ANY AI Agents with Reinforcement Learning', 'Sections 3–4', 'https://arxiv.org/abs/2508.03680'),
    source('hcapo', 'Hindsight Credit Assignment for Long-Horizon LLM Agents', 'Sections 2–4', 'https://arxiv.org/abs/2603.08754'),
  ],
}

const chapter = (id, chapterSources, zh, en) => assertFoundationChapterDefinition({
  id,
  sources: chapterSources,
  zh: { ...zh, explorer: {} },
  en: { ...en, explorer: {} },
})

export const dpoChapter = chapter('dpo', sources.dpo, {
  prerequisite: '前置：偏好数据、KL 正则与序列 log-probability', eyebrow: '离线偏好优化', title: 'DPO 为什么能直接从 chosen/rejected 回答更新策略？',
  intro: 'Direct Preference Optimization（DPO，直接偏好优化）面对的是固定偏好数据：同一个 prompt 下，一条回答被选择，另一条被拒绝。它不先拟合奖励模型再运行在线强化学习，而是从 KL 正则化最优策略反解出隐式奖励，把成对偏好概率直接写成策略损失。',
  bridge: '关键不是记住最终 sigmoid 损失，而是看清三次消元：先求 KL 正则目标的最优策略，再用策略与参考模型的对数比表示奖励，最后让同一 prompt 的配分函数在回答比较中相消。',
  derivationIntro: '下面完整保留从 KL 正则化目标到 DPO 损失的推导。每一步都说明消去了什么未知量，以及为什么同一 prompt 是不可缺少的条件。', experimentIntro: '实验固定一组偏好对，调节 β 并改变 chosen/rejected 相对参考策略的概率位移。观察偏好 margin、预测概率与损失如何同步变化。',
  interpretation: 'DPO 的系统简洁来自离线数据与解析重参数化，而不是凭空获得奖励。它擅长利用覆盖良好的偏好对，但不会主动发现数据集中从未出现的回答；β 过大还可能放大噪声偏好和长度偏差。', figure: '交互图 17.1 · Preference Pair Margin', instruction: '改变 β 与两条回答的相对概率，观察偏好边界', question: 'DPO 去掉 reward model 后，奖励信息究竟去了哪里？',
  prelude: [
    { id: 'why-offline', kicker: '问题来源', title: '在线 RLHF 很灵活，但四模型 rollout 链路并非所有任务都需要', paragraphs: ['如果已经拥有高质量 chosen/rejected 偏好对，重新在线采样、训练 reward model 和 Critic 会增加系统成本。问题是：能否在不丢失参考策略约束的情况下，直接用偏好对更新 policy？'], formulas: [String.raw`\mathcal D=\{(x,y_w,y_l)\}`] },
    { id: 'what-dpo', kicker: '新机制', title: '把奖励视为策略相对 reference 的隐式对数比', paragraphs: ['DPO 不宣称奖励不存在，而是利用 KL 正则最优解，把奖励改写成 policy 与 reference 的 log-ratio。成对比较让只依赖 prompt 的未知常数消失。'], formulas: [String.raw`r(x,y)\equiv\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`] },
  ],
  derivation: [
    { id: 'regularized-objective', rule: '写出带参考约束的策略目标', latex: String.raw`\max_\pi\ \mathbb E_{y\sim\pi(\cdot\mid x)}[r(x,y)]-\beta D_{\rm KL}(\pi(\cdot\mid x)\Vert\pi_{\rm ref}(\cdot\mid x))`, short: '偏好奖励推动改变，KL 阻止策略无界漂移。', detail: 'β 控制偏好收益与留在参考分布附近之间的权衡。此处对每个 prompt 分别求最优策略。', assumptions: [String.raw`\beta>0`], symbols: [[String.raw`x`, 'prompt'], [String.raw`\pi_{\rm ref}`, '冻结参考策略']] },
    { id: 'optimal-policy', rule: '用归一化约束求出指数加权最优策略', latex: String.raw`\pi^*(y\mid x)=\frac{1}{Z(x)}\pi_{\rm ref}(y\mid x)\exp\!\left(\frac{r(x,y)}{\beta}\right)`, short: '高奖励回答相对 reference 获得指数级更高权重。', detail: '拉格朗日乘子保证概率和为一，其影响收进配分函数 Z(x)。', assumptions: ['回答空间可归一化'], symbols: [[String.raw`Z(x)`, 'prompt 条件下的配分函数']] },
    { id: 'implicit-reward', rule: '对最优策略取对数并反解奖励', latex: String.raw`r(x,y)=\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`, short: '相对 reference 的策略位移就是隐式奖励。', detail: '奖励由可训练 policy、冻结 reference 与未知 prompt 常数组成。下一步通过成对比较消去常数。', assumptions: ['policy 与 reference 对回答赋非零概率'], symbols: [] },
    { id: 'pairwise-probability', rule: '把隐式奖励放进 Bradley–Terry 成对偏好模型', latex: String.raw`p(y_w\succ y_l\mid x)=\sigma\!\left(r(x,y_w)-r(x,y_l)\right)`, short: '偏好概率只依赖两条回答的奖励差。', detail: 'chosen 和 rejected 共享同一个 prompt，因此两项 β log Z(x) 完全抵消。', assumptions: ['两条回答共享 prompt', '偏好采用 logistic 成对模型'], symbols: [[String.raw`\sigma`, 'sigmoid 函数']] },
    { id: 'dpo-loss', rule: '用参数化策略替换最优策略并取负对数似然', latex: String.raw`\mathcal L_{\rm DPO}(\theta)=-\mathbb E_{\mathcal D}\log\sigma\!\left(\beta\left[\log\frac{\pi_\theta(y_w\mid x)}{\pi_{\rm ref}(y_w\mid x)}-\log\frac{\pi_\theta(y_l\mid x)}{\pi_{\rm ref}(y_l\mid x)}\right]\right)`, short: '提高 chosen 相对 reference 的位移，并压低 rejected 的位移。', detail: '梯度只更新当前 policy；reference 和离线数据保持固定。', assumptions: ['偏好数据来自固定分布'], symbols: [[String.raw`\theta`, '当前策略参数']] },
  ],
  deepening: [
    { id: 'complete-dpo', kicker: '完整算法', title: 'DPO minibatch 不需要 rollout，但仍需严格的序列概率对齐', paragraphs: ['每条训练样本包含同一 prompt 与两条完整回答。Current/reference 必须使用一致的 tokenization、mask 和长度处理，否则 margin 会混入实现差异。', '训练中只更新 current policy；reference 冻结。监控偏好准确率还不够，还应观察 KL、回答长度和留出集性能，防止模型学会数据偏差。'], formulas: [String.raw`m=\beta[(\log\pi_\theta^w-\log\pi_{\rm ref}^w)-(\log\pi_\theta^l-\log\pi_{\rm ref}^l)]`], pseudocode: ['初始化 policy θ 与冻结 reference', '读取一批同 prompt 的 chosen/rejected 对', '计算 current 对两条回答的 masked sequence log-probability', '计算 reference 对两条回答的 sequence log-probability', '构造 margin 与 −log σ(margin)', '仅更新 θ，并监控 KL、长度和验证偏好'], handoff: '离线偏好无法探索新回答，第 18 章重新引入在线成组采样。' },
    { id: 'failure-modes', kicker: '失效边界', title: '偏好噪声、长度捷径与覆盖不足会让正确公式优化错误数据', paragraphs: ['若 chosen 只是因为更长、更像标注风格或包含模板词而获选，DPO 会忠实放大这种偏差。β 过高使单个偏好标签产生过强位移；β 过低则几乎不改变 reference。', '反事实检查应固定语义，仅改变长度或格式，观察模型是否仍偏向 chosen；还应按任务类型切分验证，检查数据覆盖之外的退化。'], formulas: [String.raw`\text{offline improvement}\not\Rightarrow\text{out-of-support exploration}`], handoff: '需要可执行反馈和主动探索时，应使用在线可验证奖励。' },
  ],
  sections: [
    { id: 'data-contract', kicker: '数据契约', title: 'chosen/rejected 是条件偏好，不是两个独立评分样本', paragraphs: ['两条回答必须对应同一 prompt，偏好标签表达的是相对关系。若 prompt 不同，配分函数无法相消，DPO 推导不再成立。'], formula: String.raw`(x,y_w,y_l)\sim\mathcal D` },
    { id: 'beta-meaning', kicker: '参数含义', title: 'β 同时改变偏好 margin 的尺度与参考约束', paragraphs: ['β 决定多大的 log-ratio 差才会形成明显偏好概率。它必须结合标签噪声、模型容量和参考策略质量调节。'], formula: String.raw`p_w=\sigma(\beta\Delta)` },
    { id: 'offline-boundary', kicker: '方法边界', title: 'DPO 擅长重排已有回答，不擅长发现未采样策略', paragraphs: ['固定偏好集让训练稳定且便宜，也把探索范围锁在数据覆盖中。对于可以自动验证的新解法，在线 RL 更有价值。'], formula: String.raw`\operatorname{supp}(\pi_\theta)\ \text{is shaped by}\ \mathcal D` },
  ], summaryTitle: 'DPO 用解析重参数化换取系统简洁，也接受离线覆盖边界', summary: ['DPO 从 KL 正则化最优策略推导，而非经验拼接损失。', '成对比较消去只依赖 prompt 的配分函数。', 'Current/reference 的序列概率必须严格对齐。', '离线稳定性不能替代在线探索。'],
}, {
  prerequisite: 'Prerequisites: preference data, KL regularization, and sequence log-probability', eyebrow: 'Offline preference optimization', title: 'How does DPO update a policy directly from chosen and rejected responses?',
  intro: 'Direct Preference Optimization (DPO) starts from fixed preference data: one response is chosen and another rejected for the same prompt. Instead of fitting a reward model and running online reinforcement learning, it solves a KL-regularized optimum for an implicit reward and writes pairwise preference probability directly as a policy loss.', bridge: 'The point is not to memorize the final sigmoid loss but to follow three eliminations: solve the regularized optimum, express reward through a policy/reference log-ratio, and cancel the prompt-only partition function in a response pair.', derivationIntro: 'The complete derivation below keeps every step from the KL-regularized objective to DPO loss and states why a shared prompt is necessary.', experimentIntro: 'Hold one preference pair fixed, vary β and the chosen/rejected shifts relative to the reference, and observe margin, predicted preference probability, and loss together.', interpretation: 'DPO is simpler because of offline data and analytic reparameterization, not because reward information disappears. It uses well-covered preferences effectively but cannot discover unseen responses; excessive β can amplify noisy labels and length shortcuts.', figure: 'Interactive Figure 17.1 · Preference Pair Margin', instruction: 'Vary β and relative response probabilities to inspect the boundary', question: 'Where does reward information go when DPO removes the reward model?',
  prelude: [
    { id: 'why-offline', kicker: 'Problem', title: 'Online RLHF is flexible, but not every task needs a four-model rollout loop', paragraphs: ['With high-quality chosen/rejected pairs already available, online sampling, reward-model training, and a critic add cost. Can the policy learn directly without dropping the reference constraint?'], formulas: [String.raw`\mathcal D=\{(x,y_w,y_l)\}`] },
    { id: 'what-dpo', kicker: 'Mechanism', title: 'Treat reward as an implicit log-ratio to the reference', paragraphs: ['DPO rewrites reward using the KL-regularized optimal policy. Pairwise comparison then cancels the unknown constant that depends only on the prompt.'], formulas: [String.raw`r(x,y)\equiv\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`] },
  ],
  derivation: [
    { id: 'regularized-objective', rule: 'Write the reference-regularized policy objective', latex: String.raw`\max_\pi\ \mathbb E_{y\sim\pi(\cdot\mid x)}[r(x,y)]-\beta D_{\rm KL}(\pi(\cdot\mid x)\Vert\pi_{\rm ref}(\cdot\mid x))`, short: 'Reward drives change while KL prevents unbounded drift.', detail: 'β balances preference gain against staying near the reference, separately for each prompt.', assumptions: [String.raw`\beta>0`], symbols: [[String.raw`x`, 'prompt'], [String.raw`\pi_{\rm ref}`, 'frozen reference policy']] },
    { id: 'optimal-policy', rule: 'Solve the normalized exponential optimum', latex: String.raw`\pi^*(y\mid x)=\frac{1}{Z(x)}\pi_{\rm ref}(y\mid x)\exp\!\left(\frac{r(x,y)}{\beta}\right)`, short: 'High-reward responses receive exponentially more weight than under the reference.', detail: 'A Lagrange multiplier enforces normalization and becomes the partition function Z(x).', assumptions: ['The response space is normalizable'], symbols: [[String.raw`Z(x)`, 'prompt-conditioned partition function']] },
    { id: 'implicit-reward', rule: 'Take logs and solve for reward', latex: String.raw`r(x,y)=\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`, short: 'Policy displacement relative to the reference is an implicit reward.', detail: 'The unknown prompt constant will cancel in a pairwise comparison.', assumptions: ['Policy and reference assign nonzero probability'], symbols: [] },
    { id: 'pairwise-probability', rule: 'Insert implicit reward into a Bradley–Terry preference model', latex: String.raw`p(y_w\succ y_l\mid x)=\sigma\!\left(r(x,y_w)-r(x,y_l)\right)`, short: 'Preference depends only on a reward difference.', detail: 'Both responses share one prompt, so the two β log Z(x) terms cancel.', assumptions: ['Responses share a prompt', 'Preferences use a logistic pairwise model'], symbols: [[String.raw`\sigma`, 'sigmoid function']] },
    { id: 'dpo-loss', rule: 'Replace the optimum with a parameterized policy and take negative log-likelihood', latex: String.raw`\mathcal L_{\rm DPO}(\theta)=-\mathbb E_{\mathcal D}\log\sigma\!\left(\beta\left[\log\frac{\pi_\theta(y_w\mid x)}{\pi_{\rm ref}(y_w\mid x)}-\log\frac{\pi_\theta(y_l\mid x)}{\pi_{\rm ref}(y_l\mid x)}\right]\right)`, short: 'Raise the chosen shift and lower the rejected shift relative to the reference.', detail: 'Gradients update only the current policy; reference and data remain fixed.', assumptions: ['Preference data follow a fixed distribution'], symbols: [[String.raw`\theta`, 'current policy parameters']] },
  ],
  deepening: [
    { id: 'complete-dpo', kicker: 'Complete algorithm', title: 'A DPO minibatch avoids rollout but still requires exact sequence alignment', paragraphs: ['Each sample contains one prompt and two complete responses. Current and reference must share tokenization, masks, and length handling or the margin will include implementation artifacts.', 'Only the current policy updates. Monitor preference accuracy together with KL, response length, and held-out performance.'], formulas: [String.raw`m=\beta[(\log\pi_\theta^w-\log\pi_{\rm ref}^w)-(\log\pi_\theta^l-\log\pi_{\rm ref}^l)]`], pseudocode: ['Initialize policy θ and freeze a reference', 'Read a batch of same-prompt chosen/rejected pairs', 'Compute current masked sequence log-probabilities', 'Compute reference sequence log-probabilities', 'Build the margin and −log σ(margin)', 'Update θ only; monitor KL, length, and validation preference'], handoff: 'Offline preference cannot explore new responses; Chapter 18 restores online grouped sampling.' },
    { id: 'failure-modes', kicker: 'Failure boundary', title: 'Preference noise, length shortcuts, and missing coverage make a correct loss optimize bad evidence', paragraphs: ['If chosen answers win for length, template style, or annotation artifacts, DPO faithfully amplifies the shortcut. Large β makes each label produce a stronger displacement; tiny β barely changes the reference.', 'Counterfactual checks should preserve meaning while changing length or format, and validation should be segmented by task type.'], formulas: [String.raw`\text{offline improvement}\not\Rightarrow\text{out-of-support exploration}`], handoff: 'Executable feedback and active exploration call for online RL with verifiable rewards.' },
  ],
  sections: [
    { id: 'data-contract', kicker: 'Data contract', title: 'Chosen/rejected is a conditional preference, not two independent scores', paragraphs: ['Both responses must share one prompt. Otherwise the partition term does not cancel and the DPO derivation breaks.'], formula: String.raw`(x,y_w,y_l)\sim\mathcal D` },
    { id: 'beta-meaning', kicker: 'Parameter meaning', title: 'β scales both preference margin and reference restraint', paragraphs: ['It determines how much log-ratio separation produces a confident preference and must reflect label noise and reference quality.'], formula: String.raw`p_w=\sigma(\beta\Delta)` },
    { id: 'offline-boundary', kicker: 'Method boundary', title: 'DPO reorders covered responses better than it discovers unsampled strategies', paragraphs: ['A fixed dataset makes training cheap and stable while fixing the exploration support.'], formula: String.raw`\operatorname{supp}(\pi_\theta)\ \text{is shaped by}\ \mathcal D` },
  ], summaryTitle: 'DPO trades online exploration for analytic and system simplicity', summary: ['DPO is derived from a KL-regularized optimum.', 'Pairwise comparison cancels the prompt-only partition function.', 'Current/reference sequence probabilities require exact alignment.', 'Offline stability does not replace exploration.'],
})

export const grpoChapter = chapter('grpo', sources.grpo, {
  prerequisite: '前置：PPO 裁剪目标、序列奖励与在线 rollout', eyebrow: '在线可验证奖励', title: 'GRPO 如何用同组回答替代 Critic，并把在线探索带回后训练？', intro: 'Group Relative Policy Optimization（GRPO，组相对策略优化）对同一 prompt 在线采样多条回答，用组内平均表现构造 baseline。它保留 PPO 风格的概率比、裁剪与参考约束，却不训练单独的 value model。对于数学和代码等可自动验证任务，这类训练常称 Reinforcement Learning with Verifiable Rewards（RLVR，可验证奖励强化学习），奖励可以直接来自答案或测试结果。', bridge: 'GRPO 的关键变化不是简单删掉 Critic，而是把 baseline 从“预测该状态的期望回报”改成“同题候选的相对表现”。这降低模型角色数量，也把成本转移到多回答生成和组内奖励质量。', derivationIntro: '从成组采样开始，依次构造均值、标准差、相对优势、概率比与裁剪目标，再说明 Decoupled Clip and Dynamic Sampling Policy Optimization（DAPO，解耦裁剪与动态采样策略优化）为什么修改采样和裁剪边界。', experimentIntro: '调节组大小、奖励分布和裁剪范围。观察全对、全错或奖励接近时优势如何退化，以及动态采样为什么要过滤零方差组。', interpretation: 'GRPO 适合能够在线生成并可靠验证的任务。它省去 Critic，却没有消除 baseline：baseline 变成组统计量。组越大估计通常越稳但 rollout 越贵；零方差组没有排序信息，错误 verifier 则会把探索导向 reward hacking。', figure: '交互图 18.1 · Group Rollout Board', instruction: '改变回答组、奖励与裁剪，追踪组相对优势', question: '移除 Critic 后，GRPO 的 baseline 从哪里来？',
  prelude: [
    { id: 'why-online', kicker: '离线边界', title: '固定偏好对无法发现训练数据之外的新推理路径', paragraphs: ['当结果能由答案匹配、程序执行或形式验证自动判断时，当前策略可以不断生成新候选并获得低成本反馈。需要一种不依赖人工偏好对的在线方法。'], formulas: [String.raw`y_1,\ldots,y_G\sim\pi_{\rm old}(\cdot\mid x)`] },
    { id: 'group-baseline', kicker: '相对比较', title: '同一 prompt 的候选天然形成局部对照组', paragraphs: ['不同题目的奖励尺度可能不同，但同题回答可以直接比较。用组均值与标准差归一化，使策略更新关注相对胜负。'], formulas: [String.raw`A_i=(R_i-\mu_R)/(\sigma_R+\varepsilon)`] },
  ],
  derivation: [
    { id: 'group-sampling', rule: '用旧策略为同一 prompt 采样 G 条回答', latex: String.raw`y_i\sim\pi_{\rm old}(\cdot\mid x),\qquad i=1,\ldots,G`, short: '一组回答共享题目，但保留不同探索结果。', detail: '旧策略概率必须与实际生成一致，后续更新才能形成有效 ratio。', assumptions: [String.raw`G\ge2`], symbols: [[String.raw`G`, '每题回答数']] },
    { id: 'group-statistics', rule: '计算组奖励均值与标准差', latex: String.raw`\mu_R=\frac1G\sum_{j=1}^G R_j,\qquad\sigma_R=\sqrt{\frac1G\sum_{j=1}^G(R_j-\mu_R)^2}`, short: '组统计量成为无需学习的 baseline 与尺度。', detail: '若所有奖励相同，标准差为零且组内没有排序证据。', assumptions: ['组内奖励可比较'], symbols: [[String.raw`R_j`, '第 j 条回答的可验证奖励']] },
    { id: 'relative-advantage', rule: '把每条回答标准化为组相对优势', latex: String.raw`A_i=\frac{R_i-\mu_R}{\sigma_R+\varepsilon}`, short: '高于组均值的回答为正优势，低于均值为负。', detail: 'ε 只防止数值除零，不能创造原本不存在的区分信号。', assumptions: [String.raw`\varepsilon>0`], symbols: [[String.raw`A_i`, '回答级组相对优势']] },
    { id: 'clipped-update', rule: '用旧策略概率比限制重复更新', latex: String.raw`J_i=\min\!\left(r_iA_i,\operatorname{clip}(r_i,1-\epsilon,1+\epsilon)A_i\right)-\beta D_{\rm KL}(\pi_\theta\Vert\pi_{\rm ref})`, short: '相对优势给方向，裁剪和 KL 控制步幅。', detail: 'r_i 来自 current/old 概率；reference 仍是长期行为锚点。', assumptions: ['advantages 在 minibatch 更新中冻结'], symbols: [[String.raw`r_i`, '新旧策略概率比']] },
    { id: 'dynamic-sampling', rule: '过滤没有学习信号的组并解耦裁剪边界', latex: String.raw`0<\sum_{i=1}^G\mathbf1[R_i=1]<G,\qquad\operatorname{clip}(r_i,1-\epsilon_{\rm low},1+\epsilon_{\rm high})`, short: '动态采样保留有难度差异的题，非对称裁剪给低概率 token 更多上升空间。', detail: '这是 DAPO 类稳定化机制：它改变有效数据和更新边界，而不改变在线可验证奖励的基本逻辑。', assumptions: [String.raw`\epsilon_{\rm high}\ge\epsilon_{\rm low}`], symbols: [] },
  ],
  deepening: [
    { id: 'complete-grpo', kicker: '完整算法', title: 'GRPO 的循环把生成成本换成免 Critic 的相对 baseline', paragraphs: ['每轮先为 prompt 批次成组采样，再由 verifier 或 reward model 打分。优势在组内计算并冻结，随后进行有限轮策略更新。更新后旧组作废，必须重新生成。', '与 DPO 不同，它能探索新回答；与 PPO-based RLHF 不同，它不拟合 token value。'], formulas: [String.raw`\text{sample group}\rightarrow\text{verify}\rightarrow\text{normalize}\rightarrow\text{clip update}\rightarrow\text{refresh}`], pseudocode: ['采样 prompt 批次并冻结 old policy', '每题生成 G 条回答并保存 old log-probability', '运行 verifier 或 reward model 得到序列奖励', '计算每组 μ、σ 与相对 advantage', '在固定组上执行有限轮裁剪与 KL 更新', '发布新 policy，丢弃旧组并重新采样'], handoff: '当 verifier 来自编译器和测试框架时，这条循环变成 Coding RL。' },
    { id: 'stability-family', kicker: '稳定化族谱', title: '动态采样、非对称裁剪与长度归一化分别修复不同故障', paragraphs: ['动态采样处理全对/全错组没有梯度的问题；非对称裁剪避免低概率 token 的提升过早饱和；token-level loss normalization 防止长回答仅因 token 更多而主导梯度。', '这些机制不应合成一个“更强 GRPO”按钮。交互中应分别改变有效样本、概率边界和样本权重，观察各自作用。'], formulas: [String.raw`w_i\propto1/|y_i|`, String.raw`\epsilon_{\rm high}>\epsilon_{\rm low}`], handoff: '第 19 章将 verifier 从抽象分数具体化为编译与测试证据。' },
  ],
  sections: [
    { id: 'verifier', kicker: '反馈来源', title: '可验证奖励把主观偏好换成可执行判据', paragraphs: ['答案匹配、单元测试和形式检查可以重复计算奖励，但 verifier 覆盖不完整时仍可能被策略利用。'], formula: String.raw`R_i=\operatorname{Verify}(x,y_i)` },
    { id: 'group-cost', kicker: '成本迁移', title: '省掉 value model 不等于训练免费', paragraphs: ['每题多次生成提高了 rollout 开销。组大小影响优势方差、探索覆盖和吞吐。'], formula: String.raw`\text{rollout cost}\propto G\cdot|y|` },
    { id: 'zero-variance', kicker: '退化情形', title: '全对或全错组没有相对学习信号', paragraphs: ['零方差并非数值 bug，而是当前题目与策略组合没有提供排序证据。应重新采样、调整难度或过滤，而不是靠 ε 伪造优势。'], formula: String.raw`\sigma_R=0\Rightarrow A_i\approx0` },
  ], summaryTitle: 'GRPO 保留在线探索，以成组采样替代学习型 Critic', summary: ['组统计量是 GRPO 的 baseline。', '裁剪与 KL 仍负责限制策略漂移。', '零方差组没有学习证据。', 'DAPO 类机制分别改善采样、裁剪和长度权重。'],
}, {
  prerequisite: 'Prerequisites: PPO clipping, sequence rewards, and online rollout', eyebrow: 'Online verifiable rewards', title: 'How does GRPO replace the critic with response groups and restore online exploration?', intro: 'Group Relative Policy Optimization (GRPO) samples several responses online for one prompt and uses within-group performance as a baseline. It keeps PPO-style ratios, clipping, and reference regularization without training a separate value model. For math and code, this setting is often called Reinforcement Learning with Verifiable Rewards (RLVR), because rewards come directly from answer or execution verification.', bridge: 'GRPO does not merely delete the critic. It changes the baseline from predicted expected return to relative performance among candidates for the same prompt, moving cost into multi-response rollout and reward quality.', derivationIntro: 'Start with grouped sampling, derive group statistics and relative advantage, add clipped updates, then explain why Decoupled Clip and Dynamic Sampling Policy Optimization (DAPO) modifies sampling and clip boundaries.', experimentIntro: 'Vary group size, reward distribution, and clip range. Inspect all-correct, all-wrong, and low-variance groups and see why dynamic sampling filters zero-information groups.', interpretation: 'GRPO fits tasks that can generate and verify online. It removes a learned critic, not the baseline itself. Larger groups stabilize comparison but cost more rollout; a flawed verifier drives reward hacking.', figure: 'Interactive Figure 18.1 · Group Rollout Board', instruction: 'Change response groups, rewards, and clipping to trace relative advantage', question: 'Where does the baseline come from after GRPO removes the critic?',
  prelude: [
    { id: 'why-online', kicker: 'Offline boundary', title: 'Fixed preference pairs cannot discover new reasoning paths', paragraphs: ['When outcomes can be checked automatically, the current policy can continually generate candidates and receive inexpensive feedback. This calls for online learning without human pair labels.'], formulas: [String.raw`y_1,\ldots,y_G\sim\pi_{\rm old}(\cdot\mid x)`] },
    { id: 'group-baseline', kicker: 'Relative comparison', title: 'Candidates for one prompt form a natural local control group', paragraphs: ['Reward scales may differ across tasks, while answers to the same prompt are directly comparable. Group normalization focuses the update on relative wins and losses.'], formulas: [String.raw`A_i=(R_i-\mu_R)/(\sigma_R+\varepsilon)`] },
  ],
  derivation: [
    { id: 'group-sampling', rule: 'Sample G responses for one prompt under the old policy', latex: String.raw`y_i\sim\pi_{\rm old}(\cdot\mid x),\qquad i=1,\ldots,G`, short: 'The group shares a task while retaining exploratory variation.', detail: 'Stored old probabilities must match actual generation.', assumptions: [String.raw`G\ge2`], symbols: [[String.raw`G`, 'responses per prompt']] },
    { id: 'group-statistics', rule: 'Compute group reward mean and standard deviation', latex: String.raw`\mu_R=\frac1G\sum_{j=1}^G R_j,\qquad\sigma_R=\sqrt{\frac1G\sum_{j=1}^G(R_j-\mu_R)^2}`, short: 'Group statistics become a non-learned baseline and scale.', detail: 'Equal rewards produce zero variance and no ranking evidence.', assumptions: ['Rewards are comparable within a group'], symbols: [[String.raw`R_j`, 'verifiable reward of response j']] },
    { id: 'relative-advantage', rule: 'Normalize each response into group-relative advantage', latex: String.raw`A_i=\frac{R_i-\mu_R}{\sigma_R+\varepsilon}`, short: 'Above-average responses are positive and below-average responses negative.', detail: 'ε prevents division by zero but cannot create missing information.', assumptions: [String.raw`\varepsilon>0`], symbols: [[String.raw`A_i`, 'response-level relative advantage']] },
    { id: 'clipped-update', rule: 'Limit repeated updates with old-policy ratios', latex: String.raw`J_i=\min\!\left(r_iA_i,\operatorname{clip}(r_i,1-\epsilon,1+\epsilon)A_i\right)-\beta D_{\rm KL}(\pi_\theta\Vert\pi_{\rm ref})`, short: 'Advantage gives direction; clipping and KL bound movement.', detail: 'The reference remains a long-lived anchor.', assumptions: ['Advantages stay frozen during minibatch updates'], symbols: [[String.raw`r_i`, 'current-to-old probability ratio']] },
    { id: 'dynamic-sampling', rule: 'Filter groups without signal and decouple clip bounds', latex: String.raw`0<\sum_{i=1}^G\mathbf1[R_i=1]<G,\qquad\operatorname{clip}(r_i,1-\epsilon_{\rm low},1+\epsilon_{\rm high})`, short: 'Dynamic sampling keeps discriminative tasks; asymmetric clipping leaves room for rare tokens to rise.', detail: 'DAPO-style stabilization changes effective data and update boundaries without changing the RLVR core.', assumptions: [String.raw`\epsilon_{\rm high}\ge\epsilon_{\rm low}`], symbols: [] },
  ],
  deepening: [
    { id: 'complete-grpo', kicker: 'Complete algorithm', title: 'GRPO trades generation cost for a critic-free relative baseline', paragraphs: ['Each round samples groups, verifies them, freezes relative advantages, and performs finite policy updates. The old groups are discarded after publishing the new policy.', 'Unlike DPO it explores online; unlike PPO-based RLHF it does not fit token values.'], formulas: [String.raw`\text{sample group}\rightarrow\text{verify}\rightarrow\text{normalize}\rightarrow\text{clip update}\rightarrow\text{refresh}`], pseudocode: ['Sample prompts and freeze the old policy', 'Generate G responses per prompt and store old log-probabilities', 'Run a verifier or reward model for sequence rewards', 'Compute group mean, standard deviation, and advantage', 'Run finite clipped and KL-regularized updates', 'Publish the policy, discard groups, and sample again'], handoff: 'A compiler and test suite turn this loop into Coding RL.' },
    { id: 'stability-family', kicker: 'Stabilization family', title: 'Dynamic sampling, asymmetric clipping, and length normalization repair different failures', paragraphs: ['Dynamic sampling handles all-correct/all-wrong groups; asymmetric clipping prevents rare-token improvement from saturating too early; token-level normalization stops long responses from dominating solely by having more tokens.', 'These mechanisms should remain separate controls because they modify data selection, probability bounds, and sample weighting respectively.'], formulas: [String.raw`w_i\propto1/|y_i|`, String.raw`\epsilon_{\rm high}>\epsilon_{\rm low}`], handoff: 'Chapter 19 makes the verifier concrete through compilation and tests.' },
  ],
  sections: [
    { id: 'verifier', kicker: 'Feedback source', title: 'Verifiable reward replaces subjective preference with executable evidence', paragraphs: ['Answer matching, tests, and formal checks are reproducible, yet incomplete verifiers can still be exploited.'], formula: String.raw`R_i=\operatorname{Verify}(x,y_i)` },
    { id: 'group-cost', kicker: 'Cost shift', title: 'Removing the value model does not make training free', paragraphs: ['Several generations per task increase rollout cost; group size controls variance, coverage, and throughput.'], formula: String.raw`\text{rollout cost}\propto G\cdot|y|` },
    { id: 'zero-variance', kicker: 'Degenerate case', title: 'All-correct or all-wrong groups carry no relative learning signal', paragraphs: ['Zero variance is evidence that the task-policy combination supplies no ranking information, not a numerical bug.'], formula: String.raw`\sigma_R=0\Rightarrow A_i\approx0` },
  ], summaryTitle: 'GRPO keeps online exploration and replaces a learned critic with grouped sampling', summary: ['Group statistics form the GRPO baseline.', 'Clipping and KL still limit policy drift.', 'Zero-variance groups contain no learning evidence.', 'DAPO-style mechanisms separately improve sampling, clipping, and length weighting.'],
})

const compactChapter = (id, chapterSources, zh, en) => chapter(id, chapterSources, zh, en)

export const codingRlChapter = compactChapter('codingrl', sources.coding, {
  prerequisite: '前置：在线 rollout、可验证奖励与组相对优势', eyebrow: 'Coding RL', title: '编译器和测试反馈怎样把代码生成变成可验证的强化学习？', intro: '代码任务的特殊之处是输出可以执行。语法检查、编译结果、单元测试和隐藏测试共同构成环境反馈，但它们并不天然等价于一个可靠奖励。Coding RL 必须决定状态包含什么、何时执行、怎样把部分成功变成奖励，以及如何阻止策略只适配可见测试。', bridge: '从“答案对不对”进入代码后，奖励不再是单一布尔值。测试覆盖、失败类型和迭代修复提供更密集的证据，也引入测试泄漏、奖励投机和沙箱成本。', derivationIntro: '从程序执行结果开始，把测试通过向量聚合为稀疏或稠密奖励，再把执行反馈写入下一轮状态，形成多步修复轨迹。', experimentIntro: '选择三个候选补丁，切换全通过奖励、部分测试奖励和难度加权奖励。再显示隐藏测试，观察“可见测试满分”为什么不等于真实正确。', interpretation: '可执行反馈让 Coding RL 比开放式文本更容易获得客观奖励，但 verifier 的覆盖决定了模型学到的是功能正确还是测试捷径。多轮反馈只有在下一步状态真正包含错误信息时，才训练了修复能力而非独立重采样。', figure: '交互图 19.1 · Executable Feedback Lab', instruction: '比较候选补丁、奖励聚合与隐藏测试', question: '测试通过率什么时候是有效奖励，什么时候只是可被利用的代理？',
  prelude: [
    { id: 'code-as-action', kicker: '可执行动作', title: '一段代码既是 token 序列，也是会改变环境结果的复合动作', paragraphs: ['模型提交代码后，执行环境返回语法错误、异常、测试通过向量和运行资源。状态因此可以从 prompt 扩展为 prompt、当前代码与最近反馈。'], formulas: [String.raw`S_t=(x,c_t,f_t),\qquad A_t=\Delta c_t`]},
    { id: 'reward-design', kicker: '奖励选择', title: '从 pass/fail 到部分成功，每种聚合都定义了不同优化目标', paragraphs: ['只有全部测试通过才给 1 最贴近最终目标但极稀疏；按通过测试比例给分更稠密，却可能鼓励只攻克大量简单测试。难度加权试图平衡两者。'], formulas: [String.raw`R_{\rm sparse}=\mathbf1[\forall k:z_k=1],\qquad R_{\rm dense}=\sum_k w_kz_k`] },
  ],
  derivation: [
    { id: 'test-vector', rule: '把执行结果表示为测试通过向量', latex: String.raw`\mathbf z(c)=(z_1,\ldots,z_K),\qquad z_k\in\{0,1\}`, short: '每个测试提供一个可复现的局部判据。', detail: '编译失败可视为所有功能测试未通过，也可单列为反馈类型。', assumptions: ['测试在隔离环境中确定执行'], symbols: [[String.raw`K`, '测试数量']] },
    { id: 'outcome-reward', rule: '用全通过定义最终功能正确奖励', latex: String.raw`R_{\rm outcome}(c)=\prod_{k=1}^K z_k`, short: '任何测试失败都会使最终奖励为零。', detail: '目标忠实但信号稀疏，早期策略很难区分接近正确和完全错误。', assumptions: ['测试集充分代表规范'], symbols: [] },
    { id: 'partial-reward', rule: '用测试通过比例提供稠密反馈', latex: String.raw`R_{\rm partial}(c)=\frac1K\sum_{k=1}^K z_k`, short: '部分成功产生梯度方向。', detail: '大量重复或简单测试会主导奖励，因此比例不等于真实任务进度。', assumptions: ['各测试等权'], symbols: [] },
    { id: 'weighted-reward', rule: '按测试难度或区分度加权', latex: String.raw`R_{\rm weighted}(c)=\frac{\sum_{k=1}^Kw_kz_k}{\sum_{k=1}^Kw_k}`, short: '罕见通过的困难测试获得更高权重。', detail: '权重可以由历史通过率估计，但随策略变化时必须避免目标快速漂移。', assumptions: [String.raw`w_k>0`], symbols: [[String.raw`w_k`, '测试 k 的难度权重']] },
    { id: 'feedback-transition', rule: '把执行反馈写回下一状态以训练修复', latex: String.raw`S_{t+1}=(x,c_t,\operatorname{Exec}(c_t)),\qquad c_{t+1}\sim\pi_\theta(\cdot\mid S_{t+1})`, short: '模型下一步看到错误和测试结果，才能学习迭代改进。', detail: '若每次都只看原始 prompt，所谓多次尝试只是独立采样，不是利用反馈。', assumptions: ['执行反馈被准确序列化'], symbols: [[String.raw`\operatorname{Exec}`, '编译与测试环境']] },
  ],
  deepening: [
    { id: 'complete-coding-loop', kicker: '完整算法', title: 'Coding RL 把生成、沙箱执行、反馈解析与策略更新连成闭环', paragraphs: ['每条 rollout 需要保存代码、执行配置、测试版本、结果向量和旧策略概率。相同代码在不同依赖或随机测试下可能产生不同奖励，因此环境版本也是训练证据。', '策略更新可采用 PPO/GRPO 等在线目标；Coding RL 的独特部分是可执行环境与奖励契约。'], formulas: [String.raw`\text{prompt}\rightarrow\text{code}\rightarrow\text{sandbox}\rightarrow\mathbf z\rightarrow\text{reward/update}`], pseudocode: ['固定题目、依赖、测试与资源限制', '由 old policy 生成一个或一组候选代码', '在隔离沙箱中编译并执行测试', '记录失败类型、测试向量、耗时与旧概率', '聚合奖励并计算 advantage 或组相对信号', '更新 policy，刷新 rollout 并用隐藏测试验证'], handoff: '真实软件工程还要求模型跨多次工具调用维护状态。' },
    { id: 'reward-hacking', kicker: '反事实失败', title: '可见测试满分仍可能通过硬编码、泄漏或未覆盖分支作弊', paragraphs: ['如果策略能读到测试实现，它可能返回特定常量；如果测试覆盖不足，它可能忽略边界情况；如果奖励包含代码长度，它可能牺牲正确性换取短输出。', '隐藏测试、变形测试、静态规则与人工审计是互补防线，但每项都可能增加成本或引入新偏差。'], formulas: [String.raw`R_{\rm visible}=1\not\Rightarrow R_{\rm hidden}=1`], handoff: '第 20 章把单次代码回答扩展成跨仓库工具调用的 Agent 轨迹。' },
  ],
  sections: [
    { id: 'feedback-types', kicker: '反馈层级', title: '语法、运行、局部测试与隐藏测试回答不同问题', paragraphs: ['语法成功只证明程序可解析；可见测试验证已知行为；隐藏测试估计泛化。把它们压成一个分数会丢失诊断信息。'], formula: String.raw`f=(f_{\rm compile},f_{\rm runtime},\mathbf z_{\rm visible},\mathbf z_{\rm hidden})` },
    { id: 'iterative-repair', kicker: '多步改进', title: '执行反馈必须成为下一状态，而不是日志附件', paragraphs: ['真正的 repair policy 会根据具体失败修改代码。交互应展示同一轨迹中错误如何缩小，而不是并排展示独立候选。'], formula: String.raw`c_t\xrightarrow{\operatorname{Exec}}f_t\xrightarrow{\pi_\theta}c_{t+1}` },
    { id: 'safety-boundary', kicker: '系统边界', title: '任意代码执行必须被资源与权限隔离', paragraphs: ['教学网站不执行用户任意代码；实验使用预定义候选及确定测试矩阵，保持无状态和安全。'], formula: String.raw`\operatorname{Exec}:\text{sandboxed and bounded}` },
  ], summaryTitle: 'Coding RL 的核心不是换一个优化器，而是建立可信执行反馈', summary: ['测试向量是比单一分数更完整的证据。', '稀疏与稠密奖励存在忠实度和可学性的权衡。', '执行反馈进入下一状态才形成迭代修复。', '隐藏测试与沙箱是训练契约的一部分。'],
}, {
  prerequisite: 'Prerequisites: online rollout, verifiable rewards, and group-relative advantage', eyebrow: 'Coding RL', title: 'How do compilers and tests turn code generation into verifiable reinforcement learning?', intro: 'Code is executable. Syntax checks, compilation, unit tests, and hidden tests form environment feedback, but they do not automatically define a trustworthy reward. Coding RL must choose the state, execution timing, partial-credit rule, and defenses against visible-test overfitting.', bridge: 'Beyond a binary answer, test coverage, failure types, and iterative repair provide denser evidence while introducing leakage, reward gaming, and sandbox cost.', derivationIntro: 'Represent execution as a test vector, derive sparse and dense rewards, then feed execution feedback into the next state to form a repair trajectory.', experimentIntro: 'Choose among three patches, switch all-pass, partial-test, and difficulty-weighted rewards, then reveal hidden tests to see why visible success is not functional correctness.', interpretation: 'Execution offers objective feedback, but verifier coverage decides whether the model learns correctness or a shortcut. Multi-turn training teaches repair only when the next state contains the observed error.', figure: 'Interactive Figure 19.1 · Executable Feedback Lab', instruction: 'Compare patches, reward aggregation, and hidden tests', question: 'When is test pass rate a valid reward and when is it an exploitable proxy?',
  prelude: [
    { id: 'code-as-action', kicker: 'Executable action', title: 'Code is both a token sequence and a compound action that changes environment outcomes', paragraphs: ['After submission, the environment returns syntax errors, exceptions, a test vector, and resource use. State expands to prompt, current code, and recent feedback.'], formulas: [String.raw`S_t=(x,c_t,f_t),\qquad A_t=\Delta c_t`]},
    { id: 'reward-design', kicker: 'Reward choice', title: 'All-pass and partial-credit aggregations define different objectives', paragraphs: ['All-pass reward is faithful but sparse. Test proportion is dense but may favor many easy tests. Difficulty weighting attempts to balance the two.'], formulas: [String.raw`R_{\rm sparse}=\mathbf1[\forall k:z_k=1],\qquad R_{\rm dense}=\sum_k w_kz_k`] },
  ],
  derivation: [
    { id: 'test-vector', rule: 'Represent execution as a test-pass vector', latex: String.raw`\mathbf z(c)=(z_1,\ldots,z_K),\qquad z_k\in\{0,1\}`, short: 'Each test is a reproducible local criterion.', detail: 'Compilation failure can fail all tests or remain a separate feedback type.', assumptions: ['Tests run deterministically in isolation'], symbols: [[String.raw`K`, 'number of tests']] },
    { id: 'outcome-reward', rule: 'Define functional outcome reward by passing every test', latex: String.raw`R_{\rm outcome}(c)=\prod_{k=1}^K z_k`, short: 'Any failed test makes the final reward zero.', detail: 'Faithful but sparse, especially for early policies.', assumptions: ['Tests represent the specification'], symbols: [] },
    { id: 'partial-reward', rule: 'Use pass proportion for dense feedback', latex: String.raw`R_{\rm partial}(c)=\frac1K\sum_{k=1}^K z_k`, short: 'Partial success creates direction.', detail: 'Repeated easy tests can dominate the reward.', assumptions: ['Tests are equally weighted'], symbols: [] },
    { id: 'weighted-reward', rule: 'Weight tests by difficulty or discrimination', latex: String.raw`R_{\rm weighted}(c)=\frac{\sum_{k=1}^Kw_kz_k}{\sum_{k=1}^Kw_k}`, short: 'Rare success on hard tests receives more weight.', detail: 'Online difficulty estimates must not drift too rapidly.', assumptions: [String.raw`w_k>0`], symbols: [[String.raw`w_k`, 'difficulty weight']] },
    { id: 'feedback-transition', rule: 'Write execution feedback into the next state for repair', latex: String.raw`S_{t+1}=(x,c_t,\operatorname{Exec}(c_t)),\qquad c_{t+1}\sim\pi_\theta(\cdot\mid S_{t+1})`, short: 'The next action sees the actual failure.', detail: 'Attempts conditioned only on the original prompt are independent resampling, not feedback use.', assumptions: ['Feedback is serialized accurately'], symbols: [[String.raw`\operatorname{Exec}`, 'compiler and test environment']] },
  ],
  deepening: [
    { id: 'complete-coding-loop', kicker: 'Complete algorithm', title: 'Coding RL closes generation, sandbox execution, feedback parsing, and policy update', paragraphs: ['A rollout stores code, environment version, test vector, failure type, and old probability. Identical code can behave differently under dependencies or randomized tests.', 'PPO or GRPO may optimize the policy; the executable environment and reward contract define Coding RL.'], formulas: [String.raw`\text{prompt}\rightarrow\text{code}\rightarrow\text{sandbox}\rightarrow\mathbf z\rightarrow\text{reward/update}`], pseudocode: ['Freeze task, dependencies, tests, and resource limits', 'Generate one or a group of candidates under the old policy', 'Compile and run tests in an isolated sandbox', 'Record failures, test vector, runtime, and old probabilities', 'Aggregate reward and compute advantage', 'Update policy and validate on hidden tests'], handoff: 'Real software engineering requires state across several tool calls.' },
    { id: 'reward-hacking', kicker: 'Counterfactual failure', title: 'Visible-test success can come from hard-coding, leakage, or uncovered branches', paragraphs: ['A policy may read tests and return constants, ignore edge cases, or sacrifice correctness for a style reward.', 'Hidden, metamorphic, and static tests plus review are complementary defenses with their own costs.'], formulas: [String.raw`R_{\rm visible}=1\not\Rightarrow R_{\rm hidden}=1`], handoff: 'Chapter 20 expands one answer into a repository-level agent trajectory.' },
  ],
  sections: [
    { id: 'feedback-types', kicker: 'Feedback levels', title: 'Syntax, runtime, visible, and hidden tests answer different questions', paragraphs: ['Compressing them into one scalar loses diagnostic information.'], formula: String.raw`f=(f_{\rm compile},f_{\rm runtime},\mathbf z_{\rm visible},\mathbf z_{\rm hidden})` },
    { id: 'iterative-repair', kicker: 'Multi-step improvement', title: 'Execution feedback must become state, not a log attachment', paragraphs: ['A repair policy changes code in response to a specific failure rather than sampling another independent answer.'], formula: String.raw`c_t\xrightarrow{\operatorname{Exec}}f_t\xrightarrow{\pi_\theta}c_{t+1}` },
    { id: 'safety-boundary', kicker: 'System boundary', title: 'Arbitrary execution must be permission- and resource-isolated', paragraphs: ['The teaching site executes no user code; it uses predefined candidates and deterministic test matrices.'], formula: String.raw`\operatorname{Exec}:\text{sandboxed and bounded}` },
  ], summaryTitle: 'Coding RL is primarily a trustworthy execution-feedback problem', summary: ['A test vector is richer evidence than a scalar.', 'Sparse and dense rewards trade faithfulness for learnability.', 'Feedback must enter state to train repair.', 'Hidden tests and sandboxing belong to the contract.'],
})

export const agentMdpChapter = compactChapter('agentmdp', sources.agent, {
  prerequisite: '前置：Token MDP、执行反馈与多步修复', eyebrow: '多轮 Agent MDP', title: '工具调用、环境观察与记忆怎样组成一条 Agent 轨迹？', intro: '语言模型成为 Agent 后，一次动作不再只是生成下一个 token。它可能选择工具、填写参数、读取环境观察、修改外部状态并决定何时结束。为了训练这种行为，必须先规定决策时刻、可见状态、动作边界和终止条件，否则“轨迹奖励”无法对应到明确策略。', bridge: 'Token MDP 适合描述单条 response；Agent MDP 需要把模型调用、工具执行和环境反馈分离。只有在这些边界稳定后，才能讨论长程奖励和信用分配。', derivationIntro: '从历史状态开始，定义结构化工具动作、环境观察、状态更新、轨迹概率和终局目标，建立多轮 Agent 的严格对象。', experimentIntro: '沿着一个仓库 Issue 的轨迹逐步展开 search、inspect、edit、test 和 repair。切换失败分支，观察相同最终结果可能来自完全不同的中间决策。', interpretation: '长轨迹的第一项难题不是优化器，而是状态与动作契约。隐藏工具错误、截断上下文或把多个决策塞进一个 action，都会让概率和奖励失去可解释对齐。', figure: '交互图 20.1 · Agent Trajectory Tree', instruction: '展开工具分支并检查状态、动作、观察与终止', question: '在多轮 Agent 中，一次可训练的 action 到底从哪里开始、在哪里结束？',
  prelude: [
    { id: 'beyond-response', kicker: '对象扩展', title: '一条 response 只改变文本；Agent 动作会改变外部环境', paragraphs: ['工具调用可能读写仓库、运行测试或查询服务。下一状态不仅是 token 前缀，还包含环境观察和被保留的记忆。'], formulas: [String.raw`S_t=(x,h_t,m_t,o_t)`] },
    { id: 'decision-boundary', kicker: '决策边界', title: '把模型输出到工具返回定义为一个环境步', paragraphs: ['策略选择结构化 action；环境执行后返回 observation。这样 log-probability、延迟和错误都能挂到具体步骤。'], formulas: [String.raw`A_t=(\text{tool}_t,\text{args}_t),\qquad O_{t+1}=E(A_t)`] },
  ],
  derivation: [
    { id: 'agent-state', rule: '定义当前可见的 Agent 状态', latex: String.raw`S_t=(x,h_t,m_t,o_t)`, short: '任务、交互历史、记忆和最新观察共同决定下一步。', detail: '状态只包含策略实际可见的信息；隐藏环境状态属于 POMDP 的潜变量。', assumptions: ['上下文构造规则固定'], symbols: [[String.raw`h_t`, '消息与工具历史'], [String.raw`m_t`, '持久或工作记忆']] },
    { id: 'structured-action', rule: '把工具选择与参数定义为结构化动作', latex: String.raw`A_t=(u_t,\alpha_t)\sim\pi_\theta(\cdot\mid S_t)`, short: '动作同时决定调用哪个工具以及怎样调用。', detail: '自然语言 reasoning 可以属于 action 生成过程，但环境真正执行的是结构化调用。', assumptions: ['工具 schema 已知'], symbols: [[String.raw`u_t`, '工具标识'], [String.raw`\alpha_t`, '工具参数']] },
    { id: 'environment-step', rule: '由环境执行动作并返回观察', latex: String.raw`O_{t+1}\sim P(\cdot\mid S_t,A_t),\qquad S_{t+1}=F(S_t,A_t,O_{t+1})`, short: '工具结果和错误成为下一次决策证据。', detail: '超时、权限错误和非确定输出都属于环境转移，而不是自动归咎于策略。', assumptions: ['转移日志可重放'], symbols: [[String.raw`F`, '上下文与记忆更新规则']] },
    { id: 'trajectory-probability', rule: '把整条工具轨迹概率分解为逐步策略概率', latex: String.raw`\pi_\theta(\tau\mid x)=\prod_{t=0}^{T}\pi_\theta(A_t\mid S_t)`, short: '每个决策点都保留自己的概率证据。', detail: '环境观察不由策略产生，因此不进入策略 log-probability。', assumptions: ['决策边界一致'], symbols: [[String.raw`\tau`, '完整 Agent 轨迹']] },
    { id: 'terminal-objective', rule: '用终止条件和轨迹奖励定义任务目标', latex: String.raw`J(\theta)=\mathbb E_{\tau\sim\pi_\theta,E}[R(\tau)],\qquad T=\min\{t:\operatorname{done}(S_t)\}`, short: '成功、失败、预算耗尽或主动结束都必须显式定义。', detail: '只有最终奖励时，中间步骤仍共享同一结果，信用分配留到下一章。', assumptions: ['终止规则与预算固定'], symbols: [[String.raw`R(\tau)`, '轨迹级任务奖励']] },
  ],
  deepening: [
    { id: 'complete-agent-rollout', kicker: '完整执行循环', title: 'Agent rollout 交替运行 policy 与工具环境，而不是连续生成一段长文本', paragraphs: ['每次 policy forward 产出一个结构化动作；执行器验证 schema、权限和预算后调用工具；观察被标准化并写回上下文。轨迹记录版本、耗时、错误和 log-probability。', '训练数据必须区分策略错误与环境故障，否则模型会因不可控超时受到惩罚。'], formulas: [String.raw`\text{policy}\rightarrow A_t\rightarrow\text{executor}\rightarrow O_{t+1}\rightarrow\text{context}`], pseudocode: ['初始化任务、工具 schema、预算和上下文', '策略根据 S_t 生成结构化工具动作并保存 log-probability', '执行器校验权限、参数与剩余预算', '工具返回 observation 或结构化错误', '更新历史与记忆并检查终止条件', '记录完整轨迹，随后评分或进入信用分配'], handoff: '轨迹对象确定后，下一章才能比较不同奖励归因方法。' },
    { id: 'partial-observability', kicker: '状态缺失', title: '上下文截断、隐藏仓库状态和外部服务让 Agent 更接近 POMDP', paragraphs: ['模型看到的消息窗口通常不是完整环境状态。文件未读取、测试未运行或旧记忆被截断时，相同可见状态可能对应不同真实情况。', '记忆写入和检索因此也是策略的一部分；但把所有历史无限塞入上下文会增加成本和噪声。'], formulas: [String.raw`O_t\sim\Omega(\cdot\mid X_t),\qquad S_t\neq X_t`], handoff: '部分可观测和长延迟共同放大信用分配难度。' },
  ],
  sections: [
    { id: 'tool-errors', kicker: '错误归属', title: '策略错误、工具错误和环境随机性必须分开记录', paragraphs: ['错误工具、错误参数属于策略；超时和服务不可用可能属于环境。混合处理会污染奖励。'], formula: String.raw`e_t\in\{e_{\rm policy},e_{\rm tool},e_{\rm env}\}` },
    { id: 'budget', kicker: '资源约束', title: '步数、token、时间和调用费用共同定义 horizon', paragraphs: ['Agent 可以成功但成本过高。预算既是终止条件，也可能进入奖励。'], formula: String.raw`C(\tau)=c_{\rm token}+c_{\rm tool}+c_{\rm time}` },
    { id: 'branching', kicker: '反事实路径', title: '相同最终失败可能来自搜索不足、错误修改或未验证', paragraphs: ['轨迹树让学习者比较在哪个分支首次失去成功机会。'], formula: String.raw`\tau^{(1)}\neq\tau^{(2)},\qquad R(\tau^{(1)})=R(\tau^{(2)})` },
  ], summaryTitle: '先定义 Agent MDP，才能可靠训练长程工具行为', summary: ['状态包含可见历史、记忆和环境观察。', '结构化工具调用是可训练 action。', '环境观察不属于策略概率。', '终止、预算和错误归属必须显式记录。'],
}, {
  prerequisite: 'Prerequisites: Token MDP, execution feedback, and multi-step repair', eyebrow: 'Multi-turn Agent MDP', title: 'How do tool calls, observations, and memory form an agent trajectory?', intro: 'An agent action is no longer just the next token. It may choose a tool, fill arguments, observe the environment, mutate external state, and decide when to stop. Training requires explicit decision points, visible state, action boundaries, and termination rules before trajectory reward can align with a policy.', bridge: 'Token MDP describes one response; Agent MDP separates model calls, tool execution, and observations. Only then can long-horizon reward and credit be discussed.', derivationIntro: 'Define history state, structured actions, environment observations, state updates, trajectory probability, and terminal objective.', experimentIntro: 'Expand a repository-issue trajectory through search, inspect, edit, test, and repair. Switch failure branches to see how identical outcomes can follow different decisions.', interpretation: 'The first long-horizon problem is the state/action contract, not the optimizer. Hidden tool failures, truncated context, or bundled decisions destroy probability and reward alignment.', figure: 'Interactive Figure 20.1 · Agent Trajectory Tree', instruction: 'Expand tool branches and inspect state, action, observation, and termination', question: 'Where does one trainable action begin and end in a multi-turn agent?',
  prelude: [
    { id: 'beyond-response', kicker: 'Object expansion', title: 'A response changes text; an agent action changes an external environment', paragraphs: ['Tools may read and write repositories, run tests, or query services. The next state includes observations and retained memory.'], formulas: [String.raw`S_t=(x,h_t,m_t,o_t)`] },
    { id: 'decision-boundary', kicker: 'Decision boundary', title: 'Define one environment step from model output to tool return', paragraphs: ['The policy chooses a structured action and the environment returns an observation, attaching probability, latency, and error to one step.'], formulas: [String.raw`A_t=(\text{tool}_t,\text{args}_t),\qquad O_{t+1}=E(A_t)`] },
  ],
  derivation: [
    { id: 'agent-state', rule: 'Define the currently visible agent state', latex: String.raw`S_t=(x,h_t,m_t,o_t)`, short: 'Task, history, memory, and latest observation determine the next decision.', detail: 'Only policy-visible information belongs in state; hidden environment state makes the process partially observable.', assumptions: ['Context construction is fixed'], symbols: [[String.raw`h_t`, 'message/tool history'], [String.raw`m_t`, 'working or persistent memory']] },
    { id: 'structured-action', rule: 'Define tool choice and arguments as a structured action', latex: String.raw`A_t=(u_t,\alpha_t)\sim\pi_\theta(\cdot\mid S_t)`, short: 'An action selects both what tool and how to call it.', detail: 'Natural-language reasoning may support generation, while the environment executes the structured call.', assumptions: ['Tool schemas are known'], symbols: [[String.raw`u_t`, 'tool identifier'], [String.raw`\alpha_t`, 'tool arguments']] },
    { id: 'environment-step', rule: 'Execute the action and return an observation', latex: String.raw`O_{t+1}\sim P(\cdot\mid S_t,A_t),\qquad S_{t+1}=F(S_t,A_t,O_{t+1})`, short: 'Tool results and errors become evidence for the next decision.', detail: 'Timeouts and nondeterministic output are environment transitions, not automatically policy failures.', assumptions: ['Transitions are replayable'], symbols: [[String.raw`F`, 'context and memory update rule']] },
    { id: 'trajectory-probability', rule: 'Factor trajectory probability across policy decisions', latex: String.raw`\pi_\theta(\tau\mid x)=\prod_{t=0}^{T}\pi_\theta(A_t\mid S_t)`, short: 'Each decision point keeps its own probability evidence.', detail: 'Environment observations are not generated by the policy.', assumptions: ['Decision boundaries are consistent'], symbols: [[String.raw`\tau`, 'complete agent trajectory']] },
    { id: 'terminal-objective', rule: 'Define task objective through termination and trajectory reward', latex: String.raw`J(\theta)=\mathbb E_{\tau\sim\pi_\theta,E}[R(\tau)],\qquad T=\min\{t:\operatorname{done}(S_t)\}`, short: 'Success, failure, exhausted budget, and voluntary stop are explicit.', detail: 'With terminal-only reward, intermediate steps share one outcome and create the next chapter’s credit problem.', assumptions: ['Termination and budgets are fixed'], symbols: [[String.raw`R(\tau)`, 'trajectory-level task reward']] },
  ],
  deepening: [
    { id: 'complete-agent-rollout', kicker: 'Complete execution loop', title: 'Agent rollout alternates policy and tools instead of generating one long string', paragraphs: ['Each policy forward emits one structured action; an executor validates schema, permissions, and budget before tool execution; the observation is normalized back into context.', 'Training data separates policy errors from environment failures.'], formulas: [String.raw`\text{policy}\rightarrow A_t\rightarrow\text{executor}\rightarrow O_{t+1}\rightarrow\text{context}`], pseudocode: ['Initialize task, tool schemas, budget, and context', 'Generate a structured action and save its log-probability', 'Validate permissions, arguments, and remaining budget', 'Execute the tool and return observation or structured error', 'Update history/memory and check termination', 'Record the trajectory for scoring and credit assignment'], handoff: 'Once the trajectory is defined, Chapter 21 can compare attribution methods.' },
    { id: 'partial-observability', kicker: 'Missing state', title: 'Context truncation, hidden repository state, and services make agents partially observable', paragraphs: ['Unread files, unrun tests, or dropped memory can make one visible context correspond to different real states.', 'Memory writes and retrieval are therefore policy decisions, but unlimited history increases cost and noise.'], formulas: [String.raw`O_t\sim\Omega(\cdot\mid X_t),\qquad S_t\neq X_t`], handoff: 'Partial observability and long delays compound credit assignment.' },
  ],
  sections: [
    { id: 'tool-errors', kicker: 'Error attribution', title: 'Policy, tool, and environment errors need separate records', paragraphs: ['Wrong tools or arguments are policy errors; service outages may be environmental.'], formula: String.raw`e_t\in\{e_{\rm policy},e_{\rm tool},e_{\rm env}\}` },
    { id: 'budget', kicker: 'Resource constraint', title: 'Steps, tokens, time, and calls jointly define the horizon', paragraphs: ['An agent may succeed too expensively; budgets are termination conditions and possible reward terms.'], formula: String.raw`C(\tau)=c_{\rm token}+c_{\rm tool}+c_{\rm time}` },
    { id: 'branching', kicker: 'Counterfactual paths', title: 'The same failure may follow insufficient search, a bad edit, or missing verification', paragraphs: ['A trajectory tree reveals the first branch that removed the chance of success.'], formula: String.raw`\tau^{(1)}\neq\tau^{(2)},\qquad R(\tau^{(1)})=R(\tau^{(2)})` },
  ], summaryTitle: 'Define the Agent MDP before training long-horizon tool behavior', summary: ['State includes visible history, memory, and observations.', 'Structured tool calls are trainable actions.', 'Environment observations are not policy probability.', 'Termination, budget, and error attribution must be explicit.'],
})

export const creditChapter = compactChapter('credit', sources.credit, {
  prerequisite: '前置：多轮 Agent 轨迹、Return、TD 与 Advantage', eyebrow: '长程信用分配', title: '最终成功应该怎样分配给很早以前的关键决策？', intro: '长程 Agent 往往只在任务结束时得到成功或失败，但中间包含搜索、阅读、计划、工具调用、修改和验证。把同一个最终奖励广播给所有步骤会奖励无关动作，也会惩罚本来正确但被后续破坏的决策。信用分配要估计每一步对最终结果的边际贡献。', bridge: '经典折扣回报传播“之后发生了什么”，却不自动回答“这一步造成了什么”。过程奖励、分段 return、价值模型与 hindsight（事后）critic 分别引入不同额外假设。', derivationIntro: '从终局广播开始，比较折扣回报、过程奖励、分段优势和事后反事实归因，观察信息增加的同时引入了哪些偏差来源。', experimentIntro: '对同一条 search→inspect→edit→test→repair 轨迹切换四种归因方式。拖动折扣和过程奖励可信度，观察哪个步骤获得正负信用。', interpretation: '更稠密的信用不一定更真实。过程标注可能错误，价值模型可能在分布外失准，事后 critic 可能用结果倒推一个看似合理但虚假的原因。可靠方案需要把任务成功、局部证据和反事实一致性共同验证。', figure: '交互图 21.1 · Credit Assignment Microscope', instruction: '在同一轨迹上比较终局、折扣、过程与事后信用', question: '怎样区分“发生在成功之前”和“促成了成功”？',
  prelude: [
    { id: 'sparse-delay', kicker: '稀疏延迟', title: '终局奖励对所有早期步骤都相同，无法区分关键与无关动作', paragraphs: ['一条成功轨迹中可能包含绕路，一条失败轨迹中也可能包含正确搜索。仅看结果会把轨迹内差异抹平。'], formulas: [String.raw`R_0=\cdots=R_{T-1}=0,\qquad R_T\in\{0,1\}`] },
    { id: 'credit-question', kicker: '归因目标', title: '信用是动作相对合理替代的边际价值', paragraphs: ['理想情况下，应比较采取当前动作与在相同状态采取其他动作的结果差异；但真实环境通常无法枚举反事实。'], formulas: [String.raw`A(S_t,A_t)=Q(S_t,A_t)-V(S_t)`] },
  ],
  derivation: [
    { id: 'terminal-broadcast', rule: '最简单地把最终结果广播给所有步骤', latex: String.raw`\widehat A_t^{\rm terminal}=R(\tau),\qquad t=0,\ldots,T`, short: '实现简单，但所有动作得到相同方向。', detail: '它无法惩罚成功轨迹中的绕路，也无法保护失败轨迹中的正确前缀。', assumptions: ['轨迹内所有步骤共同负责'], symbols: [] },
    { id: 'discounted-return', rule: '用折扣降低远期结果对早期步骤的权重', latex: String.raw`G_t=\sum_{k=t}^{T}\gamma^{k-t}R_k`, short: '越靠近结果的步骤获得更强信号。', detail: '折扣表达时间偏好，不证明较近步骤更有因果责任。', assumptions: [String.raw`0\le\gamma\le1`], symbols: [[String.raw`\gamma`, '时间折扣']] },
    { id: 'process-reward', rule: '为可检查的中间里程碑加入过程奖励', latex: String.raw`G_t^{\rm process}=\sum_{k=t}^{T}\gamma^{k-t}(R_k^{\rm outcome}+\eta R_k^{\rm process})`, short: '测试改善、定位正确文件等局部证据提供稠密信号。', detail: 'η 决定信任过程判据的程度；错误过程奖励会系统性引导错误策略。', assumptions: [String.raw`\eta\ge0`], symbols: [[String.raw`R_k^{\rm process}`, '步骤级过程奖励']] },
    { id: 'segment-advantage', rule: '在关键决策边界估计分段价值差', latex: String.raw`\widehat A_j=\widehat Q(S_{t_j},A_{t_j})-\widehat V(S_{t_j})`, short: '只在工具调用或子目标边界分配信用，减少 token 级噪声。', detail: '分段需要稳定的边界与足够数据训练价值估计。', assumptions: ['关键决策点可识别'], symbols: [[String.raw`t_j`, '第 j 个决策边界']] },
    { id: 'hindsight-credit', rule: '在看到完整结果后评估动作的反事实必要性', latex: String.raw`C_t^{\rm hind}=\widehat Q_{\rm post}(S_t,A_t,\tau_{>t})-\widehat Q_{\rm post}(S_t,\widetilde A_t,\tau_{>t})`, short: 'Post-hoc critic 比较实际动作与合理替代动作。', detail: '它能利用失败后的完整证据，但也可能产生事后合理化，必须用重放或独立 verifier 校验。', assumptions: ['可构造合理替代动作'], symbols: [[String.raw`\widetilde A_t`, '反事实替代动作']] },
  ],
  deepening: [
    { id: 'solution-toolbox', kicker: '解法工具箱', title: '长程任务通常需要同时解决探索、反馈密度和估计偏差', paragraphs: ['难度递进让策略先在短轨迹获得成功，再逐步增加 horizon；轨迹分段把信用放在工具级决策；过程 verifier 提供局部证据；hindsight critic 利用完整结果重估关键步骤。', '这些方法解决不同问题：难度递进增加成功样本，过程奖励增加密度，价值/critic 降低方差，反事实归因提高选择性。'], formulas: [String.raw`\text{long-horizon learning}=\text{exploration}+\text{credit}+\text{reliable feedback}`], pseudocode: ['定义工具级决策边界和终局任务判据', '从短 horizon 或高成功率任务建立初始策略', '收集成功与失败轨迹并保存局部执行证据', '计算 outcome、process 与 segment-level signals', '用重放或反事实检查审计高信用步骤', '更新策略并逐步提高任务长度与难度'], handoff: '任何单一标量都不足以证明因果信用，必须保留可审计轨迹证据。' },
    { id: 'bias-audit', kicker: '偏差审计', title: '稠密反馈降低方差，却可能把错误判断更快写进策略', paragraphs: ['过程奖励模型可能偏爱冗长解释；价值模型可能高估熟悉工具；hindsight critic 可能在知道结果后虚构原因。', '审计应比较不同归因方法在同一轨迹上的一致步骤，并用真实重试或环境重放验证高价值反事实。'], formulas: [String.raw`\operatorname{Var}(\widehat A)\downarrow\ \not\Rightarrow\ \operatorname{Bias}(\widehat A)\downarrow`], handoff: '到这里，Bellman 的未来价值已经被连接到真实 Agent 中可审计的长期责任。' },
  ],
  sections: [
    { id: 'curriculum', kicker: '探索解法', title: '难度递进通过控制任务长度提高早期成功率', paragraphs: ['先训练短、可验证任务，再增加步骤、工具和环境噪声，使策略有机会观察正回报。'], formula: String.raw`H_1<H_2<\cdots<H_n` },
    { id: 'segmentation', kicker: '尺度选择', title: '工具级分段通常比 token 级广播更接近真实决策', paragraphs: ['一次 search 或 edit 内部包含许多 token，但环境只在工具边界发生可观察转移。'], formula: String.raw`\tau=\tau_1\oplus\cdots\oplus\tau_M` },
    { id: 'hindsight-risk', kicker: '事后风险', title: '知道答案后的解释不等于当时可获得的证据', paragraphs: ['Hindsight 归因必须限制 critic 使用的信息，并通过环境重放验证。'], formula: String.raw`I_{\rm critic}(t)\subseteq I_{\rm audit}` },
  ], summaryTitle: '长程信用分配是在稀疏结果、稠密代理与反事实偏差间权衡', summary: ['终局广播混淆轨迹中的关键与无关步骤。', '折扣只表达时间距离，不直接证明因果。', '过程奖励和分段价值增加密度但引入模型偏差。', 'Hindsight 信用需要反事实重放或独立验证。'],
}, {
  prerequisite: 'Prerequisites: multi-turn trajectories, returns, TD, and advantage', eyebrow: 'Long-horizon credit assignment', title: 'How should final success be assigned to decisions made much earlier?', intro: 'Long-horizon agents may receive only terminal success or failure after search, reading, planning, tools, edits, and verification. Broadcasting one outcome to every step rewards irrelevant actions and punishes good decisions later undone. Credit assignment estimates each decision’s marginal contribution.', bridge: 'Discounted return propagates what happened later, not automatically what caused it. Process rewards, segmented returns, value models, and hindsight (post-hoc) critics add different assumptions.', derivationIntro: 'Compare terminal broadcast, discounting, process rewards, segment advantage, and post-hoc counterfactuals while tracking each new source of information and bias.', experimentIntro: 'Apply four attribution schemes to one search→inspect→edit→test→repair trajectory. Change discount and process confidence to see which steps receive positive or negative credit.', interpretation: 'Denser credit is not necessarily truer. Process labels can be wrong, values can fail out of distribution, and a post-hoc critic can rationalize after seeing the result. Reliable attribution combines task outcome, local evidence, and counterfactual validation.', figure: 'Interactive Figure 21.1 · Credit Assignment Microscope', instruction: 'Compare terminal, discounted, process, and post-hoc credit on one trajectory', question: 'How do we distinguish happening before success from causing success?',
  prelude: [
    { id: 'sparse-delay', kicker: 'Sparse delay', title: 'Terminal reward gives every early step the same outcome', paragraphs: ['A successful trajectory can contain detours and a failed trajectory can contain a correct search. Outcome alone erases within-trajectory differences.'], formulas: [String.raw`R_0=\cdots=R_{T-1}=0,\qquad R_T\in\{0,1\}`] },
    { id: 'credit-question', kicker: 'Attribution target', title: 'Credit is the marginal value of an action against reasonable alternatives', paragraphs: ['Ideally compare the chosen action with alternatives in the same state, but real environments rarely enumerate those counterfactuals.'], formulas: [String.raw`A(S_t,A_t)=Q(S_t,A_t)-V(S_t)`] },
  ],
  derivation: [
    { id: 'terminal-broadcast', rule: 'Broadcast terminal outcome to every step', latex: String.raw`\widehat A_t^{\rm terminal}=R(\tau),\qquad t=0,\ldots,T`, short: 'Simple, but every action receives one direction.', detail: 'It cannot punish detours in success or protect good prefixes in failure.', assumptions: ['All steps share responsibility'], symbols: [] },
    { id: 'discounted-return', rule: 'Discount the influence of distant outcomes', latex: String.raw`G_t=\sum_{k=t}^{T}\gamma^{k-t}R_k`, short: 'Steps closer to outcome receive stronger signal.', detail: 'Temporal proximity is not proof of causal responsibility.', assumptions: [String.raw`0\le\gamma\le1`], symbols: [[String.raw`\gamma`, 'temporal discount']] },
    { id: 'process-reward', rule: 'Add process reward for checkable milestones', latex: String.raw`G_t^{\rm process}=\sum_{k=t}^{T}\gamma^{k-t}(R_k^{\rm outcome}+\eta R_k^{\rm process})`, short: 'Test improvement and correct localization add dense evidence.', detail: 'η expresses trust in process criteria; wrong process reward systematically trains the wrong policy.', assumptions: [String.raw`\eta\ge0`], symbols: [[String.raw`R_k^{\rm process}`, 'step-level process reward']] },
    { id: 'segment-advantage', rule: 'Estimate value difference at key decision boundaries', latex: String.raw`\widehat A_j=\widehat Q(S_{t_j},A_{t_j})-\widehat V(S_{t_j})`, short: 'Credit at tool or subgoal boundaries reduces token noise.', detail: 'Segmentation requires stable boundaries and value data.', assumptions: ['Key decisions are identifiable'], symbols: [[String.raw`t_j`, 'decision boundary j']] },
    { id: 'hindsight-credit', rule: 'After observing the result, estimate counterfactual necessity', latex: String.raw`C_t^{\rm hind}=\widehat Q_{\rm post}(S_t,A_t,\tau_{>t})-\widehat Q_{\rm post}(S_t,\widetilde A_t,\tau_{>t})`, short: 'A post-hoc critic compares actual and plausible alternative actions.', detail: 'It uses complete evidence but can rationalize; replay or an independent verifier must audit it.', assumptions: ['A plausible alternative action can be constructed'], symbols: [[String.raw`\widetilde A_t`, 'counterfactual action']] },
  ],
  deepening: [
    { id: 'solution-toolbox', kicker: 'Solution toolbox', title: 'Long horizons require separate solutions for exploration, feedback density, and estimation bias', paragraphs: ['Curriculum raises early success; segmentation locates tool decisions; process verifiers add local evidence; hindsight critics re-evaluate key steps with complete outcomes.', 'These solve different failures and should not be collapsed into one score.'], formulas: [String.raw`\text{long-horizon learning}=\text{exploration}+\text{credit}+\text{reliable feedback}`], pseudocode: ['Define tool-level boundaries and terminal criteria', 'Start with short or high-success tasks', 'Collect success/failure trajectories with local evidence', 'Compute outcome, process, and segment-level signals', 'Audit high-credit steps by replay or counterfactual checks', 'Update policy and increase horizon and difficulty'], handoff: 'No scalar proves causal credit without auditable trajectory evidence.' },
    { id: 'bias-audit', kicker: 'Bias audit', title: 'Dense feedback lowers variance but can write wrong judgments into the policy faster', paragraphs: ['A process model may prefer verbose reasoning, a value model familiar tools, and a hindsight critic a story invented after seeing the result.', 'Compare attribution methods on the same trajectory and replay high-value counterfactuals.'], formulas: [String.raw`\operatorname{Var}(\widehat A)\downarrow\ \not\Rightarrow\ \operatorname{Bias}(\widehat A)\downarrow`], handoff: 'This closes the loop from Bellman value to auditable responsibility in real agents.' },
  ],
  sections: [
    { id: 'curriculum', kicker: 'Exploration solution', title: 'Curriculum controls difficulty to raise early success', paragraphs: ['Train short, verifiable tasks first, then add steps, tools, and noise.'], formula: String.raw`H_1<H_2<\cdots<H_n` },
    { id: 'segmentation', kicker: 'Scale choice', title: 'Tool-level segments are closer to decisions than token broadcast', paragraphs: ['A search or edit contains many tokens while the environment changes at the tool boundary.'], formula: String.raw`\tau=\tau_1\oplus\cdots\oplus\tau_M` },
    { id: 'hindsight-risk', kicker: 'Hindsight risk', title: 'Evidence available after the answer was not available at decision time', paragraphs: ['Hindsight critics must have bounded information and replay validation.'], formula: String.raw`I_{\rm critic}(t)\subseteq I_{\rm audit}` },
  ], summaryTitle: 'Long-horizon credit balances sparse outcomes, dense proxies, and counterfactual bias', summary: ['Terminal broadcast confuses key and irrelevant steps.', 'Discount is temporal, not automatically causal.', 'Process and segment values add density and model bias.', 'Hindsight credit requires replay or independent verification.'],
})

Object.assign(dpoChapter.zh, { title: '从 chosen/rejected 回答直接优化策略' })
Object.assign(dpoChapter.en, { title: 'Direct policy optimization from chosen and rejected responses' })
Object.assign(grpoChapter.zh, { title: '以同组回答替代 Critic 的在线策略优化' })
Object.assign(grpoChapter.en, { title: 'Online policy optimization with response groups instead of a critic' })
Object.assign(codingRlChapter.zh, { title: '以编译器和测试反馈构造可验证强化学习' })
Object.assign(codingRlChapter.en, { title: 'Verifiable reinforcement learning from compiler and test feedback' })
Object.assign(agentMdpChapter.zh, { title: '工具调用、环境观察与记忆构成 Agent 轨迹' })
Object.assign(agentMdpChapter.en, { title: 'Agent trajectories composed of tool calls, observations, and memory' })
Object.assign(creditChapter.zh, { title: '将终局结果归因于长程轨迹中的关键决策' })
Object.assign(creditChapter.en, { title: 'Assigning terminal outcomes to key decisions in long trajectories' })
