import { assertFoundationChapterDefinition } from './schema.js'

const source = (id, label, pages, href) => ({ id, label, pages, href })

const sources = {
  dpo: [source('dpo', 'Direct Preference Optimization: Your Language Model is Secretly a Reward Model', 'Sections 3–5', 'https://arxiv.org/abs/2305.18290')],
  grpo: [
    source('grpo', 'DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models', 'Section 4', 'https://arxiv.org/abs/2402.03300'),
    source('dapo', 'DAPO: An Open-Source LLM Reinforcement Learning System at Scale', 'Sections 2–3', 'https://arxiv.org/abs/2503.14476'),
  ],
  coding: [
    source('rlef', 'RLEF: Grounding Code LLMs in Execution Feedback with Reinforcement Learning', 'Sections 2–4', 'https://arxiv.org/abs/2410.02089'),
    source('swerl', 'SWE-RL: Advancing LLM Reasoning via Reinforcement Learning on Open Software Evolution', 'Sections 2–4', 'https://arxiv.org/abs/2502.18449'),
  ],
  agent: [source('agent-lightning', 'Agent Lightning: Train ANY AI Agents with Reinforcement Learning', 'Sections 2–4', 'https://arxiv.org/abs/2508.03680')],
  credit: [
    source('agent-lightning', 'Agent Lightning: Train ANY AI Agents with Reinforcement Learning', 'Sections 3–4', 'https://arxiv.org/abs/2508.03680'),
    source('hcapo', 'Hindsight Credit Assignment for Long-Horizon LLM Agents', 'Sections 2–4', 'https://arxiv.org/abs/2603.08754'),
  ],
}

const chapter = (id, chapterSources, zh, en) => assertFoundationChapterDefinition({
  id,
  sources: chapterSources,
  zh: { ...zh, explorer: {} },
  en: { ...en, explorer: {} },
})

export const dpoChapter = chapter('dpo', sources.dpo, {
  prerequisite: '前置：偏好数据、KL 正则与序列 log-probability', eyebrow: '离线偏好优化', title: 'DPO 为什么能直接从 chosen/rejected 回答更新策略？',
  intro: 'Direct Preference Optimization（DPO，直接偏好优化）面对的是固定偏好数据：同一个 prompt 下，一条回答被选择，另一条被拒绝。它不先拟合奖励模型再运行在线强化学习，而是从 KL 正则化最优策略反解出隐式奖励，把成对偏好概率直接写成策略损失。',
  bridge: '关键不是记住最终 sigmoid 损失，而是看清三次消元：先求 KL 正则目标的最优策略，再用策略与参考模型的对数比表示奖励，最后让同一 prompt 的配分函数在回答比较中相消。',
  derivationIntro: '下面完整保留从 KL 正则化目标到 DPO 损失的推导。每一步都说明消去了什么未知量，以及为什么同一 prompt 是不可缺少的条件。', experimentIntro: '实验固定一组偏好对，调节 β 并改变 chosen/rejected 相对参考策略的概率位移。观察偏好 margin、预测概率与损失如何同步变化。',
  interpretation: 'DPO 的系统简洁来自离线数据与解析重参数化，而不是凭空获得奖励。它擅长利用覆盖良好的偏好对，但不会主动发现数据集中从未出现的回答；β 过大还可能放大噪声偏好和长度偏差。', figure: '交互图 17.1 · Preference Pair Margin', instruction: '改变 β 与两条回答的相对概率，观察偏好边界', question: 'DPO 去掉 reward model 后，奖励信息究竟去了哪里？',
  prelude: [
    { id: 'why-offline', kicker: '问题来源', title: '固定偏好数据提供了比在线 rollout 更直接的证据', paragraphs: ['高质量 chosen/rejected 数据已经记录同一 prompt 下哪条回答更受偏好。若仍重新在线采样、单独训练 reward model 并维护 Critic，会引入额外模型和数据生命周期。', 'DPO 要解决的具体问题是：能否直接利用这些成对关系更新 policy，同时保留 reference 对策略漂移的约束，而不把偏好误当成两条独立分数。'], formulas: [String.raw`\mathcal D=\{(x,y_w,y_l)\}`] },
    { id: 'what-dpo', kicker: '新机制', title: 'KL 正则最优解把奖励写成相对 reference 的策略位移', paragraphs: ['DPO 并未假设奖励消失，而是先求出带 KL 约束的最优策略，再反解奖励与 policy/reference log-ratio 的关系。', '奖励表达式还包含只依赖 prompt 的未知配分函数。chosen 与 rejected 共享 prompt，因此两者作差时该常数相消，剩余量可直接由两套策略概率计算。'], formulas: [String.raw`r(x,y)\equiv\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`] },
  ],
  derivation: [
    { id: 'regularized-objective', rule: '写出带参考约束的策略目标', latex: String.raw`\max_\pi\ \mathbb E_{y\sim\pi(\cdot\mid x)}[r(x,y)]-\beta D_{\rm KL}(\pi(\cdot\mid x)\Vert\pi_{\rm ref}(\cdot\mid x))`, short: '偏好奖励推动改变，KL 阻止策略无界漂移。', detail: 'β 控制偏好收益与留在参考分布附近之间的权衡。此处对每个 prompt 分别求最优策略。', assumptions: [String.raw`\beta>0`], symbols: [[String.raw`x`, 'prompt'], [String.raw`\pi_{\rm ref}`, '冻结参考策略']] },
    { id: 'optimal-policy', rule: '用归一化约束求出指数加权最优策略', latex: String.raw`\pi^*(y\mid x)=\frac{1}{Z(x)}\pi_{\rm ref}(y\mid x)\exp\!\left(\frac{r(x,y)}{\beta}\right)`, short: '高奖励回答相对 reference 获得指数级更高权重。', detail: '拉格朗日乘子保证概率和为一，其影响收进配分函数 Z(x)。', assumptions: ['回答空间可归一化'], symbols: [[String.raw`Z(x)`, 'prompt 条件下的配分函数']] },
    { id: 'implicit-reward', rule: '对最优策略取对数并反解奖励', latex: String.raw`r(x,y)=\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`, short: '相对 reference 的策略位移就是隐式奖励。', detail: '奖励由可训练 policy、冻结 reference 与未知 prompt 常数组成。下一步通过成对比较消去常数。', assumptions: ['policy 与 reference 对回答赋非零概率'], symbols: [] },
    { id: 'pairwise-probability', rule: '把隐式奖励放进 Bradley–Terry 成对偏好模型', latex: String.raw`p(y_w\succ y_l\mid x)=\sigma\!\left(r(x,y_w)-r(x,y_l)\right)`, short: '偏好概率只依赖两条回答的奖励差。', detail: 'chosen 和 rejected 共享同一个 prompt，因此两项 β log Z(x) 完全抵消。', assumptions: ['两条回答共享 prompt', '偏好采用 logistic 成对模型'], symbols: [[String.raw`\sigma`, 'sigmoid 函数']] },
    { id: 'dpo-loss', rule: '用参数化策略替换最优策略并取负对数似然', latex: String.raw`\mathcal L_{\rm DPO}(\theta)=-\mathbb E_{\mathcal D}\log\sigma\!\left(\beta\left[\log\frac{\pi_\theta(y_w\mid x)}{\pi_{\rm ref}(y_w\mid x)}-\log\frac{\pi_\theta(y_l\mid x)}{\pi_{\rm ref}(y_l\mid x)}\right]\right)`, short: '提高 chosen 相对 reference 的位移，并压低 rejected 的位移。', detail: '梯度只更新当前 policy；reference 和离线数据保持固定。', assumptions: ['偏好数据来自固定分布'], symbols: [[String.raw`\theta`, '当前策略参数']] },
  ],
  deepening: [
    { id: 'complete-dpo', kicker: '完整算法', title: 'DPO minibatch 不需要 rollout，但仍需严格的序列概率对齐', paragraphs: ['每条训练样本包含同一 prompt 与两条完整回答。Current/reference 必须使用一致的 tokenization、mask 和长度处理，否则 margin 会混入实现差异。', '训练中只更新 current policy；reference 冻结。监控偏好准确率还不够，还应观察 KL、回答长度和留出集性能，防止模型学会数据偏差。'], formulas: [String.raw`m=\beta[(\log\pi_\theta^w-\log\pi_{\rm ref}^w)-(\log\pi_\theta^l-\log\pi_{\rm ref}^l)]`], pseudocode: ['初始化 policy θ 与冻结 reference', '读取一批同 prompt 的 chosen/rejected 对', '计算 current 对两条回答的 masked sequence log-probability', '计算 reference 对两条回答的 sequence log-probability', '构造 margin 与 −log σ(margin)', '仅更新 θ，并监控 KL、长度和验证偏好'], handoff: '离线偏好无法探索新回答，第 18 章重新引入在线成组采样。' },
    { id: 'failure-modes', kicker: '失效边界', title: '偏好噪声、长度捷径与覆盖不足会让正确公式优化错误数据', paragraphs: ['若 chosen 只是因为更长、更像标注风格或包含模板词而获选，DPO 会忠实放大这种偏差。β 过高使单个偏好标签产生过强位移；β 过低则几乎不改变 reference。', '反事实检查应固定语义，仅改变长度或格式，观察模型是否仍偏向 chosen；还应按任务类型切分验证，检查数据覆盖之外的退化。'], formulas: [String.raw`\text{offline improvement}\not\Rightarrow\text{out-of-support exploration}`], handoff: '需要可执行反馈和主动探索时，应使用在线可验证奖励。' },
  ],
  sections: [
    { id: 'data-contract', kicker: '数据契约', title: '同一提示词下的偏好对', paragraphs: ['在 DPO 数据中，chosen 与 rejected 必须对应同一 prompt；标签只说明前者相对后者更好，并不提供可跨 prompt 比较的绝对奖励。', '如果两条回答来自不同 prompt，两项配分函数不再相同，关键消元无法成立；即使代码仍能计算损失，它也不再对应 DPO 的推导目标。'], formula: String.raw`(x,y_w,y_l)\sim\mathcal D` },
    { id: 'beta-meaning', kicker: '参数含义', title: 'β 同时设定偏好敏感度与 reference 约束尺度', paragraphs: ['β 放大 log-ratio 差进入 sigmoid 的尺度，因此决定多大的策略位移会被视为明确偏好。', '较大 β 会让标签产生更强梯度，也更容易放大噪声、长度和标注风格偏差；较小 β 更接近 reference。调节时必须同时观察偏好准确率、KL 和回答分布。'], formula: String.raw`p_w=\sigma(\beta\Delta)` },
    { id: 'offline-boundary', kicker: '方法边界', title: '离线偏好优化受训练数据覆盖限制', paragraphs: ['固定偏好集让训练稳定且便宜，但 policy 只能从数据中学到已经出现过的相对排序，无法主动请求新的回答并验证未知策略。', '当任务能够以执行结果自动判断新解法时，在线 rollout 可以把当前模型的失败和新候选重新送入训练；这正是下一章恢复在线探索的原因。'], formula: String.raw`\operatorname{supp}(\pi_\theta)\ \text{is shaped by}\ \mathcal D` },
  ], summaryTitle: 'DPO 用解析重参数化换取系统简洁，也接受离线覆盖边界', summary: ['DPO 从 KL 正则化最优策略推导，而非经验拼接损失。', '成对比较消去只依赖 prompt 的配分函数。', 'Current/reference 的序列概率必须严格对齐。', '离线稳定性不能替代在线探索。'],
}, {
  prerequisite: 'Prerequisites: preference data, KL regularization, and sequence log-probability', eyebrow: 'Offline preference optimization', title: 'How does DPO update a policy directly from chosen and rejected responses?',
  intro: 'Direct Preference Optimization (DPO) starts from fixed preference data: one response is chosen and another rejected for the same prompt. Instead of fitting a reward model and running online reinforcement learning, it solves a KL-regularized optimum for an implicit reward and writes pairwise preference probability directly as a policy loss.', bridge: 'The point is not to memorize the final sigmoid loss but to follow three eliminations: solve the regularized optimum, express reward through a policy/reference log-ratio, and cancel the prompt-only partition function in a response pair.', derivationIntro: 'The complete derivation below keeps every step from the KL-regularized objective to DPO loss and states why a shared prompt is necessary.', experimentIntro: 'Hold one preference pair fixed, vary β and the chosen/rejected shifts relative to the reference, and observe margin, predicted preference probability, and loss together.', interpretation: 'DPO is simpler because of offline data and analytic reparameterization, not because reward information disappears. It uses well-covered preferences effectively but cannot discover unseen responses; excessive β can amplify noisy labels and length shortcuts.', figure: 'Interactive Figure 17.1 · Preference Pair Margin', instruction: 'Vary β and relative response probabilities to inspect the boundary', question: 'Where does reward information go when DPO removes the reward model?',
  prelude: [
    { id: 'why-offline', kicker: 'Problem', title: 'Fixed preference data already provide more direct evidence than online rollout', paragraphs: ['High-quality chosen/rejected data record which response is preferred under the same prompt. Resampling online, fitting a separate reward model, and maintaining a critic add models and lifecycle cost.', 'DPO asks whether these pairwise relations can update the policy directly while retaining a reference constraint and without treating preference as two independent scores.'], formulas: [String.raw`\mathcal D=\{(x,y_w,y_l)\}`] },
    { id: 'what-dpo', kicker: 'Mechanism', title: 'The KL-regularized optimum expresses reward as policy displacement from the reference', paragraphs: ['DPO does not assume that reward vanishes. It solves the reference-regularized optimum and then rewrites reward through a policy/reference log-ratio.', 'The expression also contains an unknown partition function that depends only on the prompt. Chosen and rejected share that prompt, so subtraction cancels the constant and leaves quantities computable from policy probabilities.'], formulas: [String.raw`r(x,y)\equiv\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`] },
  ],
  derivation: [
    { id: 'regularized-objective', rule: 'Write the reference-regularized policy objective', latex: String.raw`\max_\pi\ \mathbb E_{y\sim\pi(\cdot\mid x)}[r(x,y)]-\beta D_{\rm KL}(\pi(\cdot\mid x)\Vert\pi_{\rm ref}(\cdot\mid x))`, short: 'Reward drives change while KL prevents unbounded drift.', detail: 'β balances preference gain against staying near the reference, separately for each prompt.', assumptions: [String.raw`\beta>0`], symbols: [[String.raw`x`, 'prompt'], [String.raw`\pi_{\rm ref}`, 'frozen reference policy']] },
    { id: 'optimal-policy', rule: 'Solve the normalized exponential optimum', latex: String.raw`\pi^*(y\mid x)=\frac{1}{Z(x)}\pi_{\rm ref}(y\mid x)\exp\!\left(\frac{r(x,y)}{\beta}\right)`, short: 'High-reward responses receive exponentially more weight than under the reference.', detail: 'A Lagrange multiplier enforces normalization and becomes the partition function Z(x).', assumptions: ['The response space is normalizable'], symbols: [[String.raw`Z(x)`, 'prompt-conditioned partition function']] },
    { id: 'implicit-reward', rule: 'Take logs and solve for reward', latex: String.raw`r(x,y)=\beta\log\frac{\pi^*(y\mid x)}{\pi_{\rm ref}(y\mid x)}+\beta\log Z(x)`, short: 'Policy displacement relative to the reference is an implicit reward.', detail: 'The unknown prompt constant will cancel in a pairwise comparison.', assumptions: ['Policy and reference assign nonzero probability'], symbols: [] },
    { id: 'pairwise-probability', rule: 'Insert implicit reward into a Bradley–Terry preference model', latex: String.raw`p(y_w\succ y_l\mid x)=\sigma\!\left(r(x,y_w)-r(x,y_l)\right)`, short: 'Preference depends only on a reward difference.', detail: 'Both responses share one prompt, so the two β log Z(x) terms cancel.', assumptions: ['Responses share a prompt', 'Preferences use a logistic pairwise model'], symbols: [[String.raw`\sigma`, 'sigmoid function']] },
    { id: 'dpo-loss', rule: 'Replace the optimum with a parameterized policy and take negative log-likelihood', latex: String.raw`\mathcal L_{\rm DPO}(\theta)=-\mathbb E_{\mathcal D}\log\sigma\!\left(\beta\left[\log\frac{\pi_\theta(y_w\mid x)}{\pi_{\rm ref}(y_w\mid x)}-\log\frac{\pi_\theta(y_l\mid x)}{\pi_{\rm ref}(y_l\mid x)}\right]\right)`, short: 'Raise the chosen shift and lower the rejected shift relative to the reference.', detail: 'Gradients update only the current policy; reference and data remain fixed.', assumptions: ['Preference data follow a fixed distribution'], symbols: [[String.raw`\theta`, 'current policy parameters']] },
  ],
  deepening: [
    { id: 'complete-dpo', kicker: 'Complete algorithm', title: 'A DPO minibatch avoids rollout but still requires exact sequence alignment', paragraphs: ['Each sample contains one prompt and two complete responses. Current and reference must share tokenization, masks, and length handling or the margin will include implementation artifacts.', 'Only the current policy updates. Monitor preference accuracy together with KL, response length, and held-out performance.'], formulas: [String.raw`m=\beta[(\log\pi_\theta^w-\log\pi_{\rm ref}^w)-(\log\pi_\theta^l-\log\pi_{\rm ref}^l)]`], pseudocode: ['Initialize policy θ and freeze a reference', 'Read a batch of same-prompt chosen/rejected pairs', 'Compute current masked sequence log-probabilities', 'Compute reference sequence log-probabilities', 'Build the margin and −log σ(margin)', 'Update θ only; monitor KL, length, and validation preference'], handoff: 'Offline preference cannot explore new responses; Chapter 18 restores online grouped sampling.' },
    { id: 'failure-modes', kicker: 'Failure boundary', title: 'Preference noise, length shortcuts, and missing coverage make a correct loss optimize bad evidence', paragraphs: ['If chosen answers win for length, template style, or annotation artifacts, DPO faithfully amplifies the shortcut. Large β makes each label produce a stronger displacement; tiny β barely changes the reference.', 'Counterfactual checks should preserve meaning while changing length or format, and validation should be segmented by task type.'], formulas: [String.raw`\text{offline improvement}\not\Rightarrow\text{out-of-support exploration}`], handoff: 'Executable feedback and active exploration call for online RL with verifiable rewards.' },
  ],
  sections: [
    { id: 'data-contract', kicker: 'Data contract', title: 'A preference pair shares one prompt', paragraphs: ['In DPO data, chosen and rejected must answer the same prompt. The label says only that the former is preferred to the latter; it is not an absolute reward comparable across prompts.', 'With different prompts, the partition terms differ and the key cancellation fails. The code could still produce a scalar loss, but it would no longer implement the DPO derivation.'], formula: String.raw`(x,y_w,y_l)\sim\mathcal D` },
    { id: 'beta-meaning', kicker: 'Parameter meaning', title: 'β sets both preference sensitivity and the reference scale', paragraphs: ['β scales the log-ratio difference entering the sigmoid and determines how much policy displacement produces a confident preference.', 'A larger value gives labels stronger gradients and can amplify noise, length, or annotation-style shortcuts; a smaller value stays closer to the reference. Preference accuracy, KL, and response distribution must be read together.'], formula: String.raw`p_w=\sigma(\beta\Delta)` },
    { id: 'offline-boundary', kicker: 'Method boundary', title: 'Offline preference optimization is limited by data coverage', paragraphs: ['A fixed dataset is stable and inexpensive, but the policy can only learn rankings among responses already represented in that evidence.', 'When execution can verify new solutions, online rollout can return current failures and novel candidates to training. The next chapter restores that exploration loop.'], formula: String.raw`\operatorname{supp}(\pi_\theta)\ \text{is shaped by}\ \mathcal D` },
  ], summaryTitle: 'DPO trades online exploration for analytic and system simplicity', summary: ['DPO is derived from a KL-regularized optimum.', 'Pairwise comparison cancels the prompt-only partition function.', 'Current/reference sequence probabilities require exact alignment.', 'Offline stability does not replace exploration.'],
})

export const grpoChapter = chapter('grpo', sources.grpo, {
  prerequisite: '前置：PPO 裁剪目标、序列奖励与在线 rollout', eyebrow: '在线可验证奖励', title: '同题回答的相对表现可以替代学习型 Critic', intro: '第 17 章的 DPO 能直接使用固定偏好对，却不能发现数据集中没有出现的新回答。数学答案、程序和形式证明则常能由规则或执行结果自动检查，因此当前策略可以不断生成候选并获得反馈。Group Relative Policy Optimization（GRPO，组相对策略优化）保留这条在线探索链，但不再训练 value model；它为同一个 prompt 生成一组回答，用组内表现估计每条回答的优势。', bridge: '移除 Critic 并不等于移除 baseline。GRPO 把“由网络预测当前状态的期望回报”换成“同题候选在当前策略下的平均表现”，从而省去 value model，同时把代价转移到多回答生成、组内方差和 verifier 的可靠性。', derivationIntro: '先固定同一 prompt、旧策略和 verifier，依次得到回答组、奖励统计量、相对优势以及 PPO 风格的裁剪目标。只有在看见全对或全错组为什么失去梯度以后，才引入动态采样与非对称裁剪。', experimentIntro: '调节组大小、奖励分布和裁剪范围，先预测哪些回答会得到正优势，再观察全对、全错或奖励接近时更新信号怎样消失，以及筛选有信息的组会改变哪些样本。', interpretation: 'GRPO 省去的是学习型 Critic，而不是比较基准。较大的回答组通常提供更稳定的相对排序，却增加生成与验证成本；零方差组没有可用排序证据，错误 verifier 则会稳定地奖励错误目标。', figure: '交互图 18.1 · Group Rollout Board', instruction: '改变回答组、奖励与裁剪，追踪组相对优势', question: '同一 prompt 的哪些奖励差真正提供了策略更新方向？',
  prelude: [
    { id: 'why-online', kicker: '离线数据留下的空白', title: '自动验证让当前策略产生的新回答也能进入训练', paragraphs: ['固定偏好对只能说明已经收集到的回答谁更好，无法评价当前策略刚刚发现的新解法。若答案匹配、程序执行或形式验证能够重复判断结果，策略就可以在线生成多个候选，并立即把成功、失败和新的推理路径送回训练。', '这种训练常称可验证奖励强化学习（Reinforcement Learning with Verifiable Rewards，RLVR）。它省去每轮人工排序，却把正确性要求转移给 verifier：检查规则必须真正覆盖任务目标，否则在线探索只会更快发现并利用规则漏洞。'], formulas: [String.raw`y_1,\ldots,y_G\sim\pi_{\rm old}(\cdot\mid x)`] },
    { id: 'group-baseline', kicker: '同题比较', title: '组均值只比较共享任务条件的候选回答', paragraphs: ['不同 prompt 的难度和奖励尺度可能相差很大，直接跨题比较会把题目差异误写进 advantage。同一个 prompt 的多条回答共享任务条件，因此它们的奖励差更接近“在这道题上，哪种生成结果优于当前策略的通常水平”。', 'GRPO 用组均值表示当前策略在该题上的平均表现，再用组内标准差刻画差异尺度。高于均值的回答得到正优势，低于均值的回答得到负优势；这个局部比较就是取代 value model 的 baseline。'], formulas: [String.raw`A_i=(R_i-\mu_R)/(\sigma_R+\varepsilon)`] },
  ],
  derivation: [
    { id: 'group-sampling', rule: '用旧策略为同一 prompt 采样 G 条回答', latex: String.raw`y_i\sim\pi_{\rm old}(\cdot\mid x),\qquad i=1,\ldots,G`, short: '一组回答共享题目，但保留不同探索结果。', detail: '旧策略概率必须与实际生成一致，后续更新才能形成有效 ratio。', assumptions: [String.raw`G\ge2`], symbols: [[String.raw`G`, '每题回答数']] },
    { id: 'group-statistics', rule: '计算组奖励均值与标准差', latex: String.raw`\begin{aligned}\mu_R&=\frac1G\sum_{j=1}^G R_j,\\ \sigma_R&=\sqrt{\frac1G\sum_{j=1}^G(R_j-\mu_R)^2}\end{aligned}`, short: '组统计量成为无需学习的 baseline 与尺度。', detail: '若所有奖励相同，标准差为零且组内没有排序证据。', assumptions: ['组内奖励可比较'], symbols: [[String.raw`R_j`, '第 j 条回答的可验证奖励']] },
    { id: 'relative-advantage', rule: '把每条回答标准化为组相对优势', latex: String.raw`A_i=\frac{R_i-\mu_R}{\sigma_R+\varepsilon}`, short: '高于组均值的回答为正优势，低于均值为负。', detail: 'ε 只防止数值除零，不能创造原本不存在的区分信号。', assumptions: [String.raw`\varepsilon>0`], symbols: [[String.raw`A_i`, '回答级组相对优势']] },
    { id: 'clipped-update', rule: '用旧策略概率比限制重复更新', latex: String.raw`\begin{aligned}c_i&=\operatorname{clip}(r_i,1-\epsilon,1+\epsilon),\\ J_i&=\min(r_iA_i,c_iA_i)-\beta D_{\rm KL}(\pi_\theta\Vert\pi_{\rm ref})\end{aligned}`, short: '相对优势给方向，裁剪和 KL 控制步幅。', detail: 'r_i 来自 current/old 概率；reference 仍是长期行为锚点。', assumptions: ['advantages 在 minibatch 更新中冻结'], symbols: [[String.raw`r_i`, '新旧策略概率比']] },
    { id: 'dynamic-sampling', rule: '先保留有组内差异的题，再分别设置上下裁剪边界', latex: String.raw`\begin{aligned}0&<\sum_{i=1}^G\mathbf1[R_i=1]<G,\\ c_i&=\operatorname{clip}(r_i,1-\epsilon_{\rm low},1+\epsilon_{\rm high})\end{aligned}`, short: '动态采样去掉全对或全错组，非对称裁剪为低概率 token 保留更大的上升空间。', detail: 'DAPO（Decoupled Clip and Dynamic Sampling Policy Optimization）把这两项作为稳定化机制；它们分别改变有效训练数据和概率更新边界，并不改变 GRPO 的组相对奖励来源。', assumptions: [String.raw`\epsilon_{\rm high}\ge\epsilon_{\rm low}`], symbols: [] },
  ],
  deepening: [
    { id: 'complete-grpo', kicker: '完整训练循环', title: '回答组只在有限更新期间有效，策略发布后必须重新生成', paragraphs: ['每轮先冻结 old policy，再为每个 prompt 生成 G 条回答并保存生成概率。Verifier 对完整回答打分，组均值和标准差把序列奖励变成冻结的相对优势；current/old ratio、裁剪和 reference KL 随后完成有限轮策略更新。', '策略发布后，旧回答组不再代表新的生成分布，因此必须丢弃并重新采样。与 DPO 相比，这条循环恢复了在线探索；与 PPO-based RLHF 相比，它不训练 token value，却为每题支付多次生成和验证成本。'], formulas: [String.raw`\begin{aligned}\text{sample group}&\rightarrow\text{verify}\rightarrow\text{normalize}\\ &\rightarrow\text{clip update}\rightarrow\text{refresh}\end{aligned}`], pseudocode: ['冻结 old policy 并采样 prompt 批次', '为每个 prompt 生成 G 条回答并保存 old log-probability', '用 verifier 或 reward model 得到每条回答的序列奖励', '计算每组均值、标准差和相对 advantage', '过滤无信息组，在固定组上执行有限轮裁剪与 KL 更新', '发布新 policy，丢弃旧组并重新采样'], handoff: '当 verifier 具体变成编译器和测试框架时，下一章将进入 Coding RL。' },
    { id: 'stability-family', kicker: '从失败现象选择机制', title: '动态采样、非对称裁剪和长度归一化各自处理一种故障', paragraphs: ['全对或全错组没有组内排序，动态采样决定哪些题进入更新；低概率 token 的上升空间可能过早被对称上界截断，非对称裁剪调整概率边界；长回答包含更多 token，长度归一化改变各条回答对总损失的权重。', '三项机制分别作用于数据选择、更新范围和样本权重，不能用一个含糊的“稳定化”解释替代。只有先指出观察到的失败，才能判断应修改哪一层。'], formulas: [String.raw`w_i\propto1/|y_i|`, String.raw`\epsilon_{\rm high}>\epsilon_{\rm low}`], handoff: '完整循环还必须说明一组回答何时产生、冻结和失效。' },
  ],
  sections: [
    { id: 'verifier', kicker: '在线反馈的前提', title: 'Verifier 把新回答变成奖励，也限定了模型真正会优化的目标', paragraphs: ['答案匹配、单元测试或形式检查可以稳定地重复计算奖励，使当前策略新生成的大量候选不再依赖人工逐条排序。每条回答的序列奖励都由同一任务判据产生，组内比较才有共同尺度。', '但是“可以执行”不等于“覆盖完整”。测试遗漏边界条件、答案检查允许投机格式或规则本身存在漏洞时，策略会优化这些缺口。隐藏检查和反作弊审计不是额外装饰，而是在线奖励成立的条件。'], formula: String.raw`R_i=\operatorname{Verify}(x,y_i)` },
    { id: 'group-cost', kicker: '成本迁移', title: '移除 value model 会把成本转移到成组生成', paragraphs: ['每个 prompt 需要生成 G 条回答才能形成局部 baseline，因此 rollout token 与验证开销近似随组大小增长。', '较大的组通常提供更稳定的相对排序和更广探索，却降低吞吐；较小的组成本低，但均值和标准差更容易被偶然样本支配。'], formula: String.raw`\text{rollout cost}\propto G\cdot|y|` },
    { id: 'zero-variance', kicker: '退化情形', title: '全对或全错组没有可利用的相对差异', paragraphs: ['组内奖励完全相同时，减去组均值后所有优势都接近零。这不是浮点错误，而是当前题目难度与策略能力组合没有产生排序证据。', '合理处理是重新采样、调整题目难度或过滤该组；在分母中增加 ε 只能防止除零，不能凭空创造学习方向。'], formula: String.raw`\sigma_R=0\Rightarrow A_i\approx0` },
  ], summaryTitle: 'GRPO 保留在线探索，以成组采样替代学习型 Critic', summary: ['组统计量是 GRPO 的 baseline。', '裁剪与 KL 仍负责限制策略漂移。', '零方差组没有学习证据。', 'DAPO 类机制分别改善采样、裁剪和长度权重。'],
}, {
  prerequisite: 'Prerequisites: PPO clipping, sequence rewards, and online rollout', eyebrow: 'Online verifiable rewards', title: 'Same-prompt relative performance can replace a learned critic', intro: 'DPO learns from fixed preference pairs but cannot discover responses absent from its dataset. Math answers, programs, and formal proofs can often be checked automatically, so the current policy can keep proposing candidates and receiving feedback. Group Relative Policy Optimization (GRPO) retains that online loop without training a value model: it samples a response group for one prompt and estimates each response’s advantage from within-group performance.', bridge: 'Removing the critic does not remove the baseline. GRPO replaces a network prediction of expected return with the current policy’s average performance on the same prompt, shifting cost toward multi-response generation, group variance, and verifier quality.', derivationIntro: 'Fix one prompt, old policy, and verifier; derive response groups, reward statistics, relative advantage, and a PPO-style clipped objective. Only after observing why all-correct or all-wrong groups lose signal do dynamic sampling and asymmetric clipping become necessary.', experimentIntro: 'Vary group size, reward distribution, and clipping. Predict positive advantages, then inspect how all-correct, all-wrong, or nearly equal rewards erase update signal and how informative-group filtering changes the batch.', interpretation: 'GRPO removes a learned critic rather than the comparison baseline. Larger groups usually stabilize ranking at higher rollout and verification cost; zero-variance groups contain no ranking evidence, and a flawed verifier consistently rewards the wrong target.', figure: 'Interactive Figure 18.1 · Group Rollout Board', instruction: 'Change response groups, rewards, and clipping to trace relative advantage', question: 'Which reward differences within one prompt actually provide an update direction?',
  prelude: [
    { id: 'why-online', kicker: 'What offline data cannot show', title: 'Automatic verification lets newly generated responses enter training', paragraphs: ['Fixed preference pairs compare only responses already collected. When answer matching, execution, or formal checks can repeatedly judge outcomes, the current policy can generate candidates online and return new failures and solutions directly to training.', 'This setting is called Reinforcement Learning with Verifiable Rewards (RLVR). It avoids fresh human ranking for every batch but transfers correctness to the verifier: incomplete checks simply teach the policy to exploit their gaps.'], formulas: [String.raw`y_1,\ldots,y_G\sim\pi_{\rm old}(\cdot\mid x)`] },
    { id: 'group-baseline', kicker: 'Same-prompt comparison', title: 'The group mean compares candidates under one shared task condition', paragraphs: ['Prompt difficulty and reward scale can differ, so cross-prompt comparison leaks task differences into advantage. Responses to one prompt share the condition and make reward differences closer to “which result beats this policy’s usual performance on this task.”', 'GRPO uses the group mean as that local baseline and the standard deviation as its scale. Above-mean responses receive positive advantage and below-mean responses negative advantage; this local comparison replaces the value model.'], formulas: [String.raw`A_i=(R_i-\mu_R)/(\sigma_R+\varepsilon)`] },
  ],
  derivation: [
    { id: 'group-sampling', rule: 'Sample G responses for one prompt under the old policy', latex: String.raw`y_i\sim\pi_{\rm old}(\cdot\mid x),\qquad i=1,\ldots,G`, short: 'The group shares a task while retaining exploratory variation.', detail: 'Stored old probabilities must match actual generation.', assumptions: [String.raw`G\ge2`], symbols: [[String.raw`G`, 'responses per prompt']] },
    { id: 'group-statistics', rule: 'Compute group reward mean and standard deviation', latex: String.raw`\begin{aligned}\mu_R&=\frac1G\sum_{j=1}^G R_j,\\ \sigma_R&=\sqrt{\frac1G\sum_{j=1}^G(R_j-\mu_R)^2}\end{aligned}`, short: 'Group statistics become a non-learned baseline and scale.', detail: 'Equal rewards produce zero variance and no ranking evidence.', assumptions: ['Rewards are comparable within a group'], symbols: [[String.raw`R_j`, 'verifiable reward of response j']] },
    { id: 'relative-advantage', rule: 'Normalize each response into group-relative advantage', latex: String.raw`A_i=\frac{R_i-\mu_R}{\sigma_R+\varepsilon}`, short: 'Above-average responses are positive and below-average responses negative.', detail: 'ε prevents division by zero but cannot create missing information.', assumptions: [String.raw`\varepsilon>0`], symbols: [[String.raw`A_i`, 'response-level relative advantage']] },
    { id: 'clipped-update', rule: 'Limit repeated updates with old-policy ratios', latex: String.raw`\begin{aligned}c_i&=\operatorname{clip}(r_i,1-\epsilon,1+\epsilon),\\ J_i&=\min(r_iA_i,c_iA_i)-\beta D_{\rm KL}(\pi_\theta\Vert\pi_{\rm ref})\end{aligned}`, short: 'Advantage gives direction; clipping and KL bound movement.', detail: 'The reference remains a long-lived anchor.', assumptions: ['Advantages stay frozen during minibatch updates'], symbols: [[String.raw`r_i`, 'current-to-old probability ratio']] },
    { id: 'dynamic-sampling', rule: 'Keep groups with internal differences, then set lower and upper clip bounds separately', latex: String.raw`\begin{aligned}0&<\sum_{i=1}^G\mathbf1[R_i=1]<G,\\ c_i&=\operatorname{clip}(r_i,1-\epsilon_{\rm low},1+\epsilon_{\rm high})\end{aligned}`, short: 'Dynamic sampling removes all-correct or all-wrong groups; asymmetric clipping leaves more room for low-probability tokens to rise.', detail: 'Decoupled Clip and Dynamic Sampling Policy Optimization (DAPO) uses these as separate stabilization mechanisms. They change effective data and probability boundaries without changing the source of GRPO’s group-relative reward.', assumptions: [String.raw`\epsilon_{\rm high}\ge\epsilon_{\rm low}`], symbols: [] },
  ],
  deepening: [
    { id: 'complete-grpo', kicker: 'Complete training loop', title: 'A response group expires after finite updates and must be regenerated', paragraphs: ['Freeze the old policy, generate G responses per prompt, and store generation probabilities. The verifier scores complete responses; group statistics produce frozen relative advantages; current/old ratios, clipping, and reference KL then drive finite policy updates.', 'After publishing the policy, old groups no longer represent the new generator and must be discarded. Unlike DPO, this loop explores online; unlike PPO-based RLHF, it does not fit token values but pays for repeated generation and verification.'], formulas: [String.raw`\begin{aligned}\text{sample group}&\rightarrow\text{verify}\rightarrow\text{normalize}\\ &\rightarrow\text{clip update}\rightarrow\text{refresh}\end{aligned}`], pseudocode: ['Freeze the old policy and sample prompts', 'Generate G responses per prompt and store old log-probabilities', 'Run a verifier or reward model for sequence rewards', 'Compute group mean, standard deviation, and relative advantage', 'Filter uninformative groups and run finite clipped, KL-regularized updates', 'Publish the policy, discard old groups, and sample again'], handoff: 'Chapter 19 makes the verifier concrete through compilation and tests.' },
    { id: 'stability-family', kicker: 'Choose a mechanism from the observed failure', title: 'Dynamic sampling, asymmetric clipping, and length normalization fix different layers', paragraphs: ['All-correct or all-wrong groups lack ranking evidence, so dynamic sampling changes data selection. Rare-token improvement may hit a symmetric upper bound too early, so asymmetric clipping changes the update range. Long responses contain more tokens, so length normalization changes sample weighting.', 'The mechanisms act on data, probability boundaries, and weights respectively. A generic “stabilization” label cannot explain which failure each one repairs.'], formulas: [String.raw`w_i\propto1/|y_i|`, String.raw`\epsilon_{\rm high}>\epsilon_{\rm low}`], handoff: 'The complete loop must still state when a response group is created, frozen, and retired.' },
  ],
  sections: [
    { id: 'verifier', kicker: 'Condition for online feedback', title: 'The verifier turns new responses into reward and defines what the policy will optimize', paragraphs: ['Answer matching, tests, or formal checks produce repeatable sequence rewards, so a large batch of newly generated candidates can be judged without human ranking. One task rule supplies a common scale for responses in the same group.', 'Executable does not mean complete. Missing edge cases, permissive formats, or exploitable rules redirect optimization toward verifier gaps. Hidden checks and anti-hacking audits are conditions for valid online reward, not optional decoration.'], formula: String.raw`R_i=\operatorname{Verify}(x,y_i)` },
    { id: 'group-cost', kicker: 'Cost shift', title: 'Removing the value model shifts cost to grouped generation', paragraphs: ['Each prompt needs G responses to define a local baseline, so rollout-token and verification cost grow roughly with group size.', 'Larger groups usually stabilize ranking and broaden exploration but reduce throughput. Smaller groups are cheaper and let chance candidates dominate their mean and variance.'], formula: String.raw`\text{rollout cost}\propto G\cdot|y|` },
    { id: 'zero-variance', kicker: 'Degenerate case', title: 'All-correct or all-wrong groups contain no usable relative difference', paragraphs: ['If every reward is equal, subtracting the group mean gives every response near-zero advantage. This reports missing ranking evidence for the current task-policy pair rather than a numerical bug.', 'Resampling, changing difficulty, or filtering is principled. Adding ε to the denominator prevents division by zero but cannot create a learning direction.'], formula: String.raw`\sigma_R=0\Rightarrow A_i\approx0` },
  ], summaryTitle: 'GRPO keeps online exploration and replaces a learned critic with grouped sampling', summary: ['Group statistics form the GRPO baseline.', 'Clipping and KL still limit policy drift.', 'Zero-variance groups contain no learning evidence.', 'DAPO-style mechanisms separately improve sampling, clipping, and length weighting.'],
})

const compactChapter = (id, chapterSources, zh, en) => chapter(id, chapterSources, zh, en)

export const codingRlChapter = compactChapter('codingrl', sources.coding, {
  prerequisite: '前置：在线 rollout、可验证奖励与组相对优势', eyebrow: 'Coding RL', title: '编译器和测试反馈怎样把代码生成变成可验证的强化学习？', intro: '代码任务的特殊之处是输出可以执行。语法检查、编译结果、单元测试和隐藏测试共同构成环境反馈，但它们并不天然等价于一个可靠奖励。Coding RL 必须决定状态包含什么、何时执行、怎样把部分成功变成奖励，以及如何阻止策略只适配可见测试。', bridge: '从“答案对不对”进入代码后，奖励不再是单一布尔值。测试覆盖、失败类型和迭代修复提供更密集的证据，也引入测试泄漏、奖励投机和沙箱成本。', derivationIntro: '从程序执行结果开始，把测试通过向量聚合为稀疏或稠密奖励，再把执行反馈写入下一轮状态，形成多步修复轨迹。', experimentIntro: '选择三个候选补丁，切换全通过奖励、部分测试奖励和难度加权奖励。再显示隐藏测试，观察“可见测试满分”为什么不等于真实正确。', interpretation: '可执行反馈让 Coding RL 比开放式文本更容易获得客观奖励，但 verifier 的覆盖决定了模型学到的是功能正确还是测试捷径。多轮反馈只有在下一步状态真正包含错误信息时，才训练了修复能力而非独立重采样。', figure: '交互图 19.1 · Executable Feedback Lab', instruction: '比较候选补丁、奖励聚合与隐藏测试', question: '测试通过率什么时候是有效奖励，什么时候只是可被利用的代理？',
  prelude: [
    { id: 'code-as-action', kicker: '可执行动作', title: '代码动作通过执行环境产生可观察后果', paragraphs: ['代码在模型内部是一串 token，但提交后会被编译、运行和测试，因而成为能够改变外部结果的复合动作。环境返回语法错误、异常、测试向量和资源消耗。', '下一次决策不能只看原始 prompt，还要看到当前代码和最近一次执行反馈。状态扩展后，模型才是在修复同一个程序，而不是反复生成互不相关的答案。'], formulas: [String.raw`S_t=(x,c_t,f_t),\qquad A_t=\Delta c_t`]},
    { id: 'reward-design', kicker: '奖励选择', title: '测试结果的聚合方式决定策略真正优化什么', paragraphs: ['只有全部测试通过才给 1，最贴近最终验收标准，却让绝大多数早期候选得到相同的零奖励。按通过测试比例给分提供更密集的改进信号。', '密集奖励也可能鼓励模型优先攻克数量多但简单的测试，而忽略少数关键边界条件。难度或重要性加权是在明确不同测试对最终目标的贡献。'], formulas: [String.raw`R_{\rm sparse}=\mathbf1[\forall k:z_k=1],\qquad R_{\rm dense}=\sum_k w_kz_k`] },
  ],
  derivation: [
    { id: 'test-vector', rule: '把执行结果表示为测试通过向量', latex: String.raw`\mathbf z(c)=(z_1,\ldots,z_K),\qquad z_k\in\{0,1\}`, short: '每个测试提供一个可复现的局部判据。', detail: '编译失败可视为所有功能测试未通过，也可单列为反馈类型。', assumptions: ['测试在隔离环境中确定执行'], symbols: [[String.raw`K`, '测试数量']] },
    { id: 'outcome-reward', rule: '用全通过定义最终功能正确奖励', latex: String.raw`R_{\rm outcome}(c)=\prod_{k=1}^K z_k`, short: '任何测试失败都会使最终奖励为零。', detail: '目标忠实但信号稀疏，早期策略很难区分接近正确和完全错误。', assumptions: ['测试集充分代表规范'], symbols: [] },
    { id: 'partial-reward', rule: '用测试通过比例提供稠密反馈', latex: String.raw`R_{\rm partial}(c)=\frac1K\sum_{k=1}^K z_k`, short: '部分成功产生梯度方向。', detail: '大量重复或简单测试会主导奖励，因此比例不等于真实任务进度。', assumptions: ['各测试等权'], symbols: [] },
    { id: 'weighted-reward', rule: '按测试难度或区分度加权', latex: String.raw`R_{\rm weighted}(c)=\frac{\sum_{k=1}^Kw_kz_k}{\sum_{k=1}^Kw_k}`, short: '罕见通过的困难测试获得更高权重。', detail: '权重可以由历史通过率估计，但随策略变化时必须避免目标快速漂移。', assumptions: [String.raw`w_k>0`], symbols: [[String.raw`w_k`, '测试 k 的难度权重']] },
    { id: 'feedback-transition', rule: '把执行反馈写回下一状态以训练修复', latex: String.raw`S_{t+1}=(x,c_t,\operatorname{Exec}(c_t)),\qquad c_{t+1}\sim\pi_\theta(\cdot\mid S_{t+1})`, short: '模型下一步看到错误和测试结果，才能学习迭代改进。', detail: '若每次都只看原始 prompt，所谓多次尝试只是独立采样，不是利用反馈。', assumptions: ['执行反馈被准确序列化'], symbols: [[String.raw`\operatorname{Exec}`, '编译与测试环境']] },
  ],
  deepening: [
    { id: 'complete-coding-loop', kicker: '完整算法', title: 'Coding RL 把生成、沙箱执行、反馈解析与策略更新连成闭环', paragraphs: ['每条 rollout 需要保存代码、执行配置、测试版本、结果向量和旧策略概率。相同代码在不同依赖或随机测试下可能产生不同奖励，因此环境版本也是训练证据。', '策略更新可采用 PPO/GRPO 等在线目标；Coding RL 的独特部分是可执行环境与奖励契约。'], formulas: [String.raw`\text{prompt}\rightarrow\text{code}\rightarrow\text{sandbox}\rightarrow\mathbf z\rightarrow\text{reward/update}`], pseudocode: ['固定题目、依赖、测试与资源限制', '由 old policy 生成一个或一组候选代码', '在隔离沙箱中编译并执行测试', '记录失败类型、测试向量、耗时与旧概率', '聚合奖励并计算 advantage 或组相对信号', '更新 policy，刷新 rollout 并用隐藏测试验证'], handoff: '真实软件工程还要求模型跨多次工具调用维护状态。' },
    { id: 'reward-hacking', kicker: '反事实失败', title: '可见测试满分仍可能通过硬编码、泄漏或未覆盖分支作弊', paragraphs: ['如果策略能读到测试实现，它可能返回特定常量；如果测试覆盖不足，它可能忽略边界情况；如果奖励包含代码长度，它可能牺牲正确性换取短输出。', '隐藏测试、变形测试、静态规则与人工审计是互补防线，但每项都可能增加成本或引入新偏差。'], formulas: [String.raw`R_{\rm visible}=1\not\Rightarrow R_{\rm hidden}=1`], handoff: '第 20 章把单次代码回答扩展成跨仓库工具调用的 Agent 轨迹。' },
  ],
  sections: [
    { id: 'feedback-types', kicker: '反馈层级', title: '不同执行信号定位不同类型的失败', paragraphs: ['语法成功只证明程序可解析；运行成功排除了即时异常；可见测试验证已知行为；隐藏测试进一步检查未暴露输入上的泛化。', '把这些信号压成一个总分会让策略无法区分“代码根本不能运行”和“只差一个边界条件”。训练记录应保留分层证据，再由奖励函数决定如何聚合。'], formula: String.raw`f=(f_{\rm compile},f_{\rm runtime},\mathbf z_{\rm visible},\mathbf z_{\rm hidden})` },
    { id: 'iterative-repair', kicker: '多步改进', title: '执行反馈连接前后两次代码决策', paragraphs: ['真正的 repair policy 根据具体失败修改现有代码：编译错误引导语法修复，失败用例定位行为差异，资源超限提示复杂度问题。', '如果反馈只是日志附件而没有进入下一状态，后续候选就无法条件化于上一次结果。交互实验应沿同一轨迹展示错误集合如何收缩，而非并排陈列独立答案。'], formula: String.raw`c_t\xrightarrow{\operatorname{Exec}}f_t\xrightarrow{\pi_\theta}c_{t+1}` },
    { id: 'safety-boundary', kicker: '系统边界', title: '可执行反馈必须在明确的权限和资源边界内产生', paragraphs: ['任意模型代码可能访问文件、网络或消耗无限资源，因此真实系统必须限制权限、时间、内存与输出，并区分程序失败和沙箱拒绝。', '本教学网站保持无状态，不执行用户任意代码；实验使用预定义候选与确定测试矩阵复现执行反馈，从而保留学习机制而不引入运行风险。'], formula: String.raw`\operatorname{Exec}:\text{sandboxed and bounded}` },
  ], summaryTitle: 'Coding RL 的核心不是换一个优化器，而是建立可信执行反馈', summary: ['测试向量是比单一分数更完整的证据。', '稀疏与稠密奖励存在忠实度和可学性的权衡。', '执行反馈进入下一状态才形成迭代修复。', '隐藏测试与沙箱是训练契约的一部分。'],
}, {
  prerequisite: 'Prerequisites: online rollout, verifiable rewards, and group-relative advantage', eyebrow: 'Coding RL', title: 'How do compilers and tests turn code generation into verifiable reinforcement learning?', intro: 'Code is executable. Syntax checks, compilation, unit tests, and hidden tests form environment feedback, but they do not automatically define a trustworthy reward. Coding RL must choose the state, execution timing, partial-credit rule, and defenses against visible-test overfitting.', bridge: 'Beyond a binary answer, test coverage, failure types, and iterative repair provide denser evidence while introducing leakage, reward gaming, and sandbox cost.', derivationIntro: 'Represent execution as a test vector, derive sparse and dense rewards, then feed execution feedback into the next state to form a repair trajectory.', experimentIntro: 'Choose among three patches, switch all-pass, partial-test, and difficulty-weighted rewards, then reveal hidden tests to see why visible success is not functional correctness.', interpretation: 'Execution offers objective feedback, but verifier coverage decides whether the model learns correctness or a shortcut. Multi-turn training teaches repair only when the next state contains the observed error.', figure: 'Interactive Figure 19.1 · Executable Feedback Lab', instruction: 'Compare patches, reward aggregation, and hidden tests', question: 'When is test pass rate a valid reward and when is it an exploitable proxy?',
  prelude: [
    { id: 'code-as-action', kicker: 'Executable action', title: 'A code action produces observable consequences through execution', paragraphs: ['Code is a token sequence inside the model, but compilation, execution, and tests make its submission a compound external action. The environment returns syntax errors, exceptions, a test vector, and resource use.', 'The next decision must include current code and recent execution feedback, not only the original prompt. That state turns generation into repair of one evolving program rather than independent answer sampling.'], formulas: [String.raw`S_t=(x,c_t,f_t),\qquad A_t=\Delta c_t`]},
    { id: 'reward-design', kicker: 'Reward choice', title: 'Test aggregation determines the objective the policy actually optimizes', paragraphs: ['An all-pass reward matches final acceptance but gives the same zero signal to most early candidates. Test-pass proportion supplies denser evidence of partial progress.', 'Dense reward can prefer solving many easy tests while ignoring a small critical edge case. Difficulty or importance weighting explicitly states how different tests contribute to the final goal.'], formulas: [String.raw`R_{\rm sparse}=\mathbf1[\forall k:z_k=1],\qquad R_{\rm dense}=\sum_k w_kz_k`] },
  ],
  derivation: [
    { id: 'test-vector', rule: 'Represent execution as a test-pass vector', latex: String.raw`\mathbf z(c)=(z_1,\ldots,z_K),\qquad z_k\in\{0,1\}`, short: 'Each test is a reproducible local criterion.', detail: 'Compilation failure can fail all tests or remain a separate feedback type.', assumptions: ['Tests run deterministically in isolation'], symbols: [[String.raw`K`, 'number of tests']] },
    { id: 'outcome-reward', rule: 'Define functional outcome reward by passing every test', latex: String.raw`R_{\rm outcome}(c)=\prod_{k=1}^K z_k`, short: 'Any failed test makes the final reward zero.', detail: 'Faithful but sparse, especially for early policies.', assumptions: ['Tests represent the specification'], symbols: [] },
    { id: 'partial-reward', rule: 'Use pass proportion for dense feedback', latex: String.raw`R_{\rm partial}(c)=\frac1K\sum_{k=1}^K z_k`, short: 'Partial success creates direction.', detail: 'Repeated easy tests can dominate the reward.', assumptions: ['Tests are equally weighted'], symbols: [] },
    { id: 'weighted-reward', rule: 'Weight tests by difficulty or discrimination', latex: String.raw`R_{\rm weighted}(c)=\frac{\sum_{k=1}^Kw_kz_k}{\sum_{k=1}^Kw_k}`, short: 'Rare success on hard tests receives more weight.', detail: 'Online difficulty estimates must not drift too rapidly.', assumptions: [String.raw`w_k>0`], symbols: [[String.raw`w_k`, 'difficulty weight']] },
    { id: 'feedback-transition', rule: 'Write execution feedback into the next state for repair', latex: String.raw`S_{t+1}=(x,c_t,\operatorname{Exec}(c_t)),\qquad c_{t+1}\sim\pi_\theta(\cdot\mid S_{t+1})`, short: 'The next action sees the actual failure.', detail: 'Attempts conditioned only on the original prompt are independent resampling, not feedback use.', assumptions: ['Feedback is serialized accurately'], symbols: [[String.raw`\operatorname{Exec}`, 'compiler and test environment']] },
  ],
  deepening: [
    { id: 'complete-coding-loop', kicker: 'Complete algorithm', title: 'Coding RL closes generation, sandbox execution, feedback parsing, and policy update', paragraphs: ['A rollout stores code, environment version, test vector, failure type, and old probability. Identical code can behave differently under dependencies or randomized tests.', 'PPO or GRPO may optimize the policy; the executable environment and reward contract define Coding RL.'], formulas: [String.raw`\text{prompt}\rightarrow\text{code}\rightarrow\text{sandbox}\rightarrow\mathbf z\rightarrow\text{reward/update}`], pseudocode: ['Freeze task, dependencies, tests, and resource limits', 'Generate one or a group of candidates under the old policy', 'Compile and run tests in an isolated sandbox', 'Record failures, test vector, runtime, and old probabilities', 'Aggregate reward and compute advantage', 'Update policy and validate on hidden tests'], handoff: 'Real software engineering requires state across several tool calls.' },
    { id: 'reward-hacking', kicker: 'Counterfactual failure', title: 'Visible-test success can come from hard-coding, leakage, or uncovered branches', paragraphs: ['A policy may read tests and return constants, ignore edge cases, or sacrifice correctness for a style reward.', 'Hidden, metamorphic, and static tests plus review are complementary defenses with their own costs.'], formulas: [String.raw`R_{\rm visible}=1\not\Rightarrow R_{\rm hidden}=1`], handoff: 'Chapter 20 expands one answer into a repository-level agent trajectory.' },
  ],
  sections: [
    { id: 'feedback-types', kicker: 'Feedback levels', title: 'Execution signals localize different failure classes', paragraphs: ['Parsing only proves syntactic validity, successful runtime rules out immediate exceptions, visible tests check known behavior, and hidden tests probe generalization to undisclosed inputs.', 'Compressing them into one scalar prevents the policy from distinguishing code that cannot run from code missing one edge case. Store layered evidence first, then define its reward aggregation.'], formula: String.raw`f=(f_{\rm compile},f_{\rm runtime},\mathbf z_{\rm visible},\mathbf z_{\rm hidden})` },
    { id: 'iterative-repair', kicker: 'Multi-step improvement', title: 'Execution feedback links successive code decisions', paragraphs: ['A repair policy changes existing code in response to a specific failure: compiler output guides syntax repair, failing cases localize behavior, and resource limits indicate complexity.', 'If feedback remains a log attachment rather than next-state information, later candidates cannot condition on the previous result. The experiment should follow one shrinking error set rather than unrelated samples.'], formula: String.raw`c_t\xrightarrow{\operatorname{Exec}}f_t\xrightarrow{\pi_\theta}c_{t+1}` },
    { id: 'safety-boundary', kicker: 'System boundary', title: 'Executable feedback requires explicit permission and resource limits', paragraphs: ['Model-generated code can access files, network, or unbounded resources, so a real executor needs permission, time, memory, and output limits and must distinguish program failure from sandbox rejection.', 'This stateless teaching site executes no arbitrary user code. Predefined candidates and deterministic test matrices preserve the learning mechanism without runtime risk.'], formula: String.raw`\operatorname{Exec}:\text{sandboxed and bounded}` },
  ], summaryTitle: 'Coding RL is primarily a trustworthy execution-feedback problem', summary: ['A test vector is richer evidence than a scalar.', 'Sparse and dense rewards trade faithfulness for learnability.', 'Feedback must enter state to train repair.', 'Hidden tests and sandboxing belong to the contract.'],
})

export const agentMdpChapter = compactChapter('agentmdp', sources.agent, {
  prerequisite: '前置：Token MDP、执行反馈与多步修复', eyebrow: '多轮 Agent MDP', title: '工具调用、环境观察与记忆构成 Agent 轨迹', intro: '第 19 章可以给一段代码回答运行测试，但真实 Coding Agent 不会只提交一次补丁：它先搜索仓库，读取文件，修改代码，运行测试，再根据错误继续修复。若把这一串过程仍当作一段长文本，训练数据就无法说明哪部分由策略选择、哪部分由工具返回，也无法把测试结果对应到具体决策。', bridge: '因此，本章先不讨论用哪种优化器，而是把一次仓库任务拆成可训练的环境步。策略在可见状态下选择一个结构化工具动作，环境执行后返回 observation，新的观察和记忆再组成下一状态；终止与预算则规定轨迹何时结束。', derivationIntro: '沿着“搜索—观察—下一决策”的实际循环，定义策略可见状态、结构化工具动作、环境转移、逐步策略概率以及任务终止目标。', experimentIntro: '实验从同一个仓库 Issue 和共享搜索前缀出发，展开 inspect、edit、test 与 stop 的真实分支。选择任一分支后，逐项核对动作、工具 observation、后续状态和终局结果，并与另一条路径比较首次分叉。', interpretation: '分支树表明，Agent 轨迹不是动作标签的线性列表，而是一系列“策略选择—环境反馈—新状态”。只有每个节点都保留这些边界，失败才能区分为错误决策、工具故障或尚未观察到的环境事实。', figure: '交互图 20.1 · Agent Trajectory Tree', instruction: '选择工具分支，检查共享前缀、动作、观察、后续状态与终止', question: '哪一次工具选择首先改变了后续可达状态？',
  prelude: [
    { id: 'beyond-response', kicker: '仓库任务中的状态变化', title: '一次工具调用会改变下一步真正面对的环境', paragraphs: ['搜索命令会暴露新的文件位置，编辑会改变仓库内容，测试则把通过项和错误栈写成新的证据。与普通 response 只增加 token 前缀不同，这些动作会改变 Agent 下一步能够观察和操作的外部状态。', '因此，训练状态不能只保存最近一段对话；它还要记录任务、已见历史、保留记忆和最新工具观察。否则两个看起来相同的上下文，可能分别对应“代码尚未修改”和“补丁已经写入但测试失败”这两种完全不同的决策处境。'], formulas: [String.raw`S_t=(x,h_t,m_t,o_t)`] },
    { id: 'decision-boundary', kicker: '一个可训练环境步', title: '策略选择工具和参数，环境负责执行并返回观察', paragraphs: ['在状态 S_t 下，策略为结构化动作分配概率：它既选择工具，也生成参数。执行器随后检查 schema、权限与预算，调用工具，并把正常输出、错误或超时写成 observation。这个 observation 不是策略生成的 token 概率，而是环境转移的结果。', '把边界固定在“选择动作—收到观察”之后，保存的 log-probability 才只对应策略可控的部分，工具延迟和服务故障也不会被误算成同一种策略错误。下一状态由旧状态、动作和 observation 共同构造，后面的奖励与信用才有明确挂点。'], formulas: [String.raw`A_t=(\text{tool}_t,\text{args}_t),\qquad O_{t+1}=E(A_t)`] },
  ],
  derivation: [
    { id: 'agent-state', rule: '定义当前可见的 Agent 状态', latex: String.raw`S_t=(x,h_t,m_t,o_t)`, short: '任务、交互历史、记忆和最新观察共同决定下一步。', detail: '状态只包含策略实际可见的信息；隐藏环境状态属于 POMDP 的潜变量。', assumptions: ['上下文构造规则固定'], symbols: [[String.raw`h_t`, '消息与工具历史'], [String.raw`m_t`, '持久或工作记忆']] },
    { id: 'structured-action', rule: '把工具选择与参数定义为结构化动作', latex: String.raw`A_t=(u_t,\alpha_t)\sim\pi_\theta(\cdot\mid S_t)`, short: '动作同时决定调用哪个工具以及怎样调用。', detail: '自然语言 reasoning 可以属于 action 生成过程，但环境真正执行的是结构化调用。', assumptions: ['工具 schema 已知'], symbols: [[String.raw`u_t`, '工具标识'], [String.raw`\alpha_t`, '工具参数']] },
    { id: 'environment-step', rule: '由环境执行动作并返回观察', latex: String.raw`\begin{aligned}O_{t+1}&\sim P(\cdot\mid S_t,A_t),\\ S_{t+1}&=F(S_t,A_t,O_{t+1}).\end{aligned}`, short: '工具结果和错误成为下一次决策证据。', detail: '超时、权限错误和非确定输出都属于环境转移，而不是自动归咎于策略。', assumptions: ['转移日志可重放'], symbols: [[String.raw`F`, '上下文与记忆更新规则']] },
    { id: 'trajectory-probability', rule: '把整条工具轨迹概率分解为逐步策略概率', latex: String.raw`\pi_\theta(\tau\mid x)=\prod_{t=0}^{T}\pi_\theta(A_t\mid S_t)`, short: '每个决策点都保留自己的概率证据。', detail: '环境观察不由策略产生，因此不进入策略 log-probability。', assumptions: ['决策边界一致'], symbols: [[String.raw`\tau`, '完整 Agent 轨迹']] },
    { id: 'terminal-objective', rule: '用终止条件和轨迹奖励定义任务目标', latex: String.raw`\begin{aligned}J(\theta)&=\mathbb E_{\tau\sim\pi_\theta,E}[R(\tau)],\\ T&=\min\{t:\operatorname{done}(S_t)\}.\end{aligned}`, short: '成功、失败、预算耗尽或主动结束都必须显式定义。', detail: '只有最终奖励时，中间步骤仍共享同一结果，信用分配留到下一章。', assumptions: ['终止规则与预算固定'], symbols: [[String.raw`R(\tau)`, '轨迹级任务奖励']] },
  ],
  deepening: [
    { id: 'complete-agent-rollout', kicker: '完整工具交互循环', title: 'Agent rollout 在每个决策点交替运行策略与环境', paragraphs: ['系统先初始化仓库任务、工具 schema、预算和上下文。每轮由 policy 生成一个结构化动作并保存 log-probability；执行器校验后调用工具，把 observation 或结构化错误写回历史和记忆，再检查成功、失败、主动 stop 或预算耗尽。', '轨迹日志同时保存模型版本、动作参数、错误来源、耗时和环境变更。这样，训练既能重放“策略当时看见了什么”，也能区分错误工具选择与不可控超时；一条长文本做不到这种逐步归属。'], formulas: [String.raw`\text{policy}\rightarrow A_t\rightarrow\text{executor}\rightarrow O_{t+1}\rightarrow\text{context}`], pseudocode: ['初始化任务、工具 schema、预算和上下文', '策略根据 S_t 生成结构化工具动作并保存 log-probability', '执行器校验权限、参数与剩余预算', '工具返回 observation 或结构化错误', '更新历史与记忆并检查终止条件', '记录完整轨迹，随后评分或进入信用分配'], handoff: '完整循环建立后，分支之间的差异才可以被可靠比较。' },
    { id: 'partial-observability', kicker: '分支树仍看不见全部环境', title: '未读取文件、截断历史和外部服务使可见状态并不等于真实状态', paragraphs: ['即使轨迹边界完整，Agent 看到的消息窗口也通常只是环境的一部分。文件尚未读取、测试尚未运行、后台服务发生变化或旧记忆被截断时，相同可见上下文仍可能对应不同真实仓库状态。', '这时问题更接近部分可观测决策过程（POMDP）：记忆写入、检索和主动检查本身也成为策略动作。无限保留历史会增加成本和噪声，而遗漏关键观察又会让后续动作在错误状态估计上发生；这一限制将在长程信用分配中继续放大。'], formulas: [String.raw`O_t\sim\Omega(\cdot\mid X_t),\qquad S_t\neq X_t`], handoff: '即使每一步都被正确定义，终局结果仍没有说明哪一个早期分支真正促成了成功。' },
  ],
  sections: [
    { id: 'tool-errors', kicker: '错误归属依赖边界', title: '错误工具选择与工具故障不能接受同一种惩罚', paragraphs: ['选择错误工具、构造无效参数或在已有失败证据后仍不修复，属于策略能够改变的决策；工具实现缺陷、权限拒绝、服务超时或外部状态突变则可能不受策略控制。完整 observation 必须标明错误来源和是否值得重试。', '若所有失败都得到相同负奖励，模型会为不可控故障承担责任，甚至学会回避本应继续使用的工具。只有先区分 policy、tool 和 environment，后面的 reward 才能表达可控行为。'], formula: String.raw`e_t\in\{e_{\rm policy},e_{\rm tool},e_{\rm env}\}` },
    { id: 'budget', kicker: '终止条件也定义目标', title: '成功路径还必须满足步数、时间与调用成本约束', paragraphs: ['一个 Agent 可以靠重复搜索、反复读取和昂贵调用最终通过测试，但这种成功与用少量关键步骤完成任务并不等价。除成功、失败和主动 stop 外，轨迹还要记录 token、工具调用、时间和费用预算。', '预算耗尽可以直接终止 episode，也可以作为 reward 中的成本项：前者把超限路径判为不可行，后者允许策略在成功概率与代价之间权衡。实验必须保留这个选择，因为它改变了要优化的任务。'], formula: String.raw`C(\tau)=c_{\rm token}+c_{\rm tool}+c_{\rm time}` },
    { id: 'branching', kicker: '共享前缀后的不同未来', title: '真实分支树用动作、观察和后续状态定位第一次关键分叉', paragraphs: ['从同一个搜索结果出发，Agent 可以先 inspect 再 edit，也可以直接修改、运行测试或过早 stop。每条边不仅有工具名称，还必须带着参数、返回 observation 和 successor state；否则所谓“分支”只是换标签的线性步骤。', '两条失败路径可能得到相同终局分数，却分别源于搜索不足、错误修改或遗漏验证。将它们与成功路径对齐到共享前缀，可以找到第一次改变后续可达状态的动作，为下一章提供比最终成败更具体的归因证据。'], formula: String.raw`\tau^{(1)}\neq\tau^{(2)},\qquad R(\tau^{(1)})=R(\tau^{(2)})` },
  ], summaryTitle: '先定义 Agent MDP，才能可靠训练长程工具行为', summary: ['状态包含可见历史、记忆和环境观察。', '结构化工具调用是可训练 action。', '环境观察不属于策略概率。', '终止、预算和错误归属必须显式记录。'],
}, {
  prerequisite: 'Prerequisites: Token MDP, execution feedback, and multi-step repair', eyebrow: 'Multi-turn Agent MDP', title: 'Tool calls, environment observations, and memory form an agent trajectory', intro: 'Chapter 19 can test one code response, but a real coding agent searches a repository, reads files, edits code, runs tests, and repairs after observing errors. Treating that sequence as one long string loses which parts were policy choices, which came from tools, and which test result belongs to which decision.', bridge: 'This chapter therefore defines trainable environment steps before choosing an optimizer. The policy selects a structured tool action from visible state; the environment executes it and returns an observation; observation and memory form the next state; termination and budgets close the trajectory.', derivationIntro: 'Follow one search–observation–decision loop to define visible state, structured action, environment transition, per-step policy probability, and the terminal task objective.', experimentIntro: 'Start from one repository issue and a shared search prefix, then expand real inspect, edit, test, and stop branches. For each branch, compare action, tool observation, successor state, terminal outcome, and first divergence from another path.', interpretation: 'The tree shows an agent trajectory as repeated policy choice, environment feedback, and new state rather than a linear list of action labels. Only explicit boundaries distinguish bad decisions from tool failures or facts not yet observed.', figure: 'Interactive Figure 20.1 · Agent Trajectory Tree', instruction: 'Choose tool branches and inspect shared prefix, action, observation, successor state, and termination', question: 'Which tool choice first changed the set of reachable future states?',
  prelude: [
    { id: 'beyond-response', kicker: 'State change in a repository task', title: 'A tool call changes the environment faced by the next decision', paragraphs: ['Search reveals file locations, edits mutate repository contents, and tests turn pass results or stack traces into new evidence. Unlike a response that only extends a token prefix, these actions change external state.', 'Training state must therefore retain the task, visible history, memory, and latest tool observation. Otherwise identical-looking context can hide two different conditions: code not yet edited versus a patch already applied and failing tests.'], formulas: [String.raw`S_t=(x,h_t,m_t,o_t)`] },
    { id: 'decision-boundary', kicker: 'One trainable environment step', title: 'The policy chooses tool and arguments; the environment executes and observes', paragraphs: ['At state S_t, the policy assigns probability to a structured action containing both tool and arguments. The executor checks schema, permission, and budget before returning output, error, or timeout as an observation. That observation is an environment result, not policy-generated probability.', 'Fixing the boundary from action selection to received observation keeps log-probability on the controllable decision and prevents latency or service faults from being confused with policy error. The next state then follows from prior state, action, and observation.'], formulas: [String.raw`A_t=(\text{tool}_t,\text{args}_t),\qquad O_{t+1}=E(A_t)`] },
  ],
  derivation: [
    { id: 'agent-state', rule: 'Define the currently visible agent state', latex: String.raw`S_t=(x,h_t,m_t,o_t)`, short: 'Task, history, memory, and latest observation determine the next decision.', detail: 'Only policy-visible information belongs in state; hidden environment state makes the process partially observable.', assumptions: ['Context construction is fixed'], symbols: [[String.raw`h_t`, 'message/tool history'], [String.raw`m_t`, 'working or persistent memory']] },
    { id: 'structured-action', rule: 'Define tool choice and arguments as a structured action', latex: String.raw`A_t=(u_t,\alpha_t)\sim\pi_\theta(\cdot\mid S_t)`, short: 'An action selects both what tool and how to call it.', detail: 'Natural-language reasoning may support generation, while the environment executes the structured call.', assumptions: ['Tool schemas are known'], symbols: [[String.raw`u_t`, 'tool identifier'], [String.raw`\alpha_t`, 'tool arguments']] },
    { id: 'environment-step', rule: 'Execute the action and return an observation', latex: String.raw`\begin{aligned}O_{t+1}&\sim P(\cdot\mid S_t,A_t),\\ S_{t+1}&=F(S_t,A_t,O_{t+1}).\end{aligned}`, short: 'Tool results and errors become evidence for the next decision.', detail: 'Timeouts and nondeterministic output are environment transitions, not automatically policy failures.', assumptions: ['Transitions are replayable'], symbols: [[String.raw`F`, 'context and memory update rule']] },
    { id: 'trajectory-probability', rule: 'Factor trajectory probability across policy decisions', latex: String.raw`\pi_\theta(\tau\mid x)=\prod_{t=0}^{T}\pi_\theta(A_t\mid S_t)`, short: 'Each decision point keeps its own probability evidence.', detail: 'Environment observations are not generated by the policy.', assumptions: ['Decision boundaries are consistent'], symbols: [[String.raw`\tau`, 'complete agent trajectory']] },
    { id: 'terminal-objective', rule: 'Define task objective through termination and trajectory reward', latex: String.raw`\begin{aligned}J(\theta)&=\mathbb E_{\tau\sim\pi_\theta,E}[R(\tau)],\\ T&=\min\{t:\operatorname{done}(S_t)\}.\end{aligned}`, short: 'Success, failure, exhausted budget, and voluntary stop are explicit.', detail: 'With terminal-only reward, intermediate steps share one outcome and create the next chapter’s credit problem.', assumptions: ['Termination and budgets are fixed'], symbols: [[String.raw`R(\tau)`, 'trajectory-level task reward']] },
  ],
  deepening: [
    { id: 'complete-agent-rollout', kicker: 'Complete tool-interaction loop', title: 'Agent rollout alternates policy and environment at every decision point', paragraphs: ['Initialize the repository task, tool schemas, budget, and context. Each round the policy emits one structured action and stores its log-probability; the executor validates and runs it; observation or structured error updates history and memory before success, failure, voluntary stop, or budget exhaustion is checked.', 'The trajectory log also stores model version, arguments, error source, latency, and environment mutation. Training can then reconstruct what the policy saw and separate a bad tool choice from an uncontrollable timeout—something one long text sequence cannot do.'], formulas: [String.raw`\text{policy}\rightarrow A_t\rightarrow\text{executor}\rightarrow O_{t+1}\rightarrow\text{context}`], pseudocode: ['Initialize task, tool schemas, budget, and context', 'Generate a structured action and save its log-probability', 'Validate permissions, arguments, and remaining budget', 'Execute the tool and return observation or structured error', 'Update history/memory and check termination', 'Record the trajectory for scoring and credit assignment'], handoff: 'Once the loop is explicit, branches can be compared reliably.' },
    { id: 'partial-observability', kicker: 'The branch tree still cannot see the whole environment', title: 'Unread files, truncated history, and external services separate visible state from real state', paragraphs: ['Even with correct step boundaries, the message window usually exposes only part of the environment. An unread file, unrun test, changed service, or dropped memory can make the same visible context correspond to different repository states.', 'The task is therefore closer to a partially observable decision process (POMDP): memory writes, retrieval, and active inspection become policy actions. Unlimited history adds cost and noise, while missing evidence produces decisions from a false state estimate; this limitation amplifies long-horizon credit difficulty.'], formulas: [String.raw`O_t\sim\Omega(\cdot\mid X_t),\qquad S_t\neq X_t`], handoff: 'Even perfectly defined steps leave terminal outcome unable to identify which early branch caused success.' },
  ],
  sections: [
    { id: 'tool-errors', kicker: 'Attribution follows the boundary', title: 'A bad tool decision and a tool failure should not receive the same penalty', paragraphs: ['Choosing the wrong tool, producing invalid arguments, or ignoring known failure evidence is policy-controllable. Tool defects, permission rejection, service timeout, or external-state change may not be. Observations need explicit error source and retryability.', 'One negative reward for every failure blames the model for uncontrollable faults and can teach it to avoid tools worth retrying. Reward becomes meaningful only after policy, tool, and environment failures are separated.'], formula: String.raw`e_t\in\{e_{\rm policy},e_{\rm tool},e_{\rm env}\}` },
    { id: 'budget', kicker: 'Termination also defines the objective', title: 'A successful path must still satisfy step, time, and call budgets', paragraphs: ['An agent can eventually pass tests through repeated search, redundant reads, or expensive calls, but that outcome differs from solving the task with a few decisive actions. The trajectory therefore records token, tool, time, and monetary budgets alongside success, failure, and voluntary stop.', 'Budget exhaustion may terminate the episode or appear as reward cost. The former marks an infeasible path; the latter trades success probability against expense. The experiment must retain that choice because it changes the task objective.'], formula: String.raw`C(\tau)=c_{\rm token}+c_{\rm tool}+c_{\rm time}` },
    { id: 'branching', kicker: 'Different futures after a shared prefix', title: 'A real branch tree locates first divergence through actions, observations, and successor states', paragraphs: ['From one search result, the agent may inspect before editing, edit immediately, run tests, or stop too early. Every edge needs arguments, observation, and successor state in addition to a tool label; otherwise the “tree” is merely a relabeled linear sequence.', 'Two failures may share terminal reward but originate from insufficient search, a bad edit, or omitted verification. Aligning them with success at a shared prefix reveals the first action that changed reachable futures and gives the next chapter more evidence than terminal outcome alone.'], formula: String.raw`\tau^{(1)}\neq\tau^{(2)},\qquad R(\tau^{(1)})=R(\tau^{(2)})` },
  ], summaryTitle: 'Define the Agent MDP before training long-horizon tool behavior', summary: ['State includes visible history, memory, and observations.', 'Structured tool calls are trainable actions.', 'Environment observations are not policy probability.', 'Termination, budget, and error attribution must be explicit.'],
})

export const creditChapter = compactChapter('credit', sources.credit, {
  prerequisite: '前置：多轮 Agent 轨迹、Return、TD 与 Advantage', eyebrow: '长程信用分配', title: '终局结果必须回到真正改变任务走向的决策', intro: '第 20 章已经把 search、inspect、edit、test 和 repair 写成清楚的环境步，但仓库任务往往仍只在最后给出成功或失败。若把成功奖励原样分给整条轨迹，绕路和关键修改得到同样强化；若最终失败，早期正确定位也会被后续错误一起惩罚。长程信用分配要判断的不是某一步是否出现在结果之前，而是它相对合理替代动作究竟改变了多少后续成功机会。', bridge: '折扣回报只加入时间距离，不能证明因果；过程奖励加入局部检查，但继承 verifier 偏差；分段价值在工具边界比较动作与状态基线；事后反事实则需要从同一状态重放替代动作。信用越细，所依赖的额外证据和假设也越多。', derivationIntro: '在同一条工具轨迹上逐级加入时间、局部验证、分段 baseline 和替代动作结果，比较每种信用信号能区分什么，以及它不能证明什么。', experimentIntro: '在同一条 search→inspect→edit→test→repair 轨迹上切换终局广播、折扣回报、过程奖励、分段优势和反事实重放。先预测哪一步应获得最高信用，再检查分段账本和同状态替代动作是否真的支持这个判断。', interpretation: '实验显示，信号更稠密不等于归因更真实。过程 verifier 可能错误，分段 baseline 可能失准，事后 critic 也可能利用决策时不可见的信息。只有替代动作能从可比状态真实重放时，结果差才更接近因果证据；否则它仍是需要审计的估计。', figure: '交互图 21.1 · Credit Assignment Microscope', instruction: '比较五种信用，并核对分段 baseline 与替代动作重放', question: '哪项证据足以说明一次早期决策真正改变了终局结果？',
  prelude: [
    { id: 'sparse-delay', kicker: '同一个结果覆盖了不同作用', title: '终局广播把关键修改、补救动作和无关绕路压成同一信号', paragraphs: ['设一条成功轨迹先搜索到正确文件，再做了一次无关读取，随后修改、测试并修复。若每一步都收到同一个成功奖励，策略无法知道正确定位和最终修复值得复现，也无法知道无关读取只是偶然出现在成功之前。失败轨迹同样可能包含正确前缀，只是被后续错误破坏。', '终局结果适合定义端到端任务目标，却没有提供轨迹内部的比较。轨迹越长，越多与当前动作无关的后续选择和环境随机性进入同一个更新，单步信用的方差和歧义也随之增加。'], formulas: [String.raw`R_0=\cdots=R_{T-1}=0,\qquad R_T\in\{0,1\}`] },
    { id: 'credit-question', kicker: '需要比较的对象', title: '一次动作的信用来自它相对可行替代动作改变的长期结果', paragraphs: ['在 edit 决策点，真正有意义的比较不是“这次修改之后任务是否成功”，而是固定当时可见状态，比较实际补丁与另一条合理补丁各自引出的后续结果。优势函数用 Q(S_t,A_t) 表示选择该动作后的长期价值，再减去同状态下通常可达到的 V(S_t)，正好刻画这种相对改进。', '真实仓库很难对每个隐藏状态穷举所有替代动作，因此后面的过程奖励、分段 value 和事后 critic 都是在近似这项未完全观测的比较。每种近似必须同时说明新增证据来自哪里、哪些量由模型估计，以及怎样防止未来信息泄漏。'], formulas: [String.raw`A(S_t,A_t)=Q(S_t,A_t)-V(S_t)`] },
  ],
  derivation: [
    { id: 'terminal-broadcast', rule: '只使用终局结果时，把同一方向分给所有工具步骤', latex: String.raw`\widehat A_t^{\rm terminal}=R(\tau),\qquad t=0,\ldots,T`, short: '它保留端到端任务目标，却不区分每一步的作用。', detail: '成功轨迹中的绕路与关键修复得到相同强化，失败轨迹中的正确前缀也得不到保护。', assumptions: ['所有步骤共同承担终局结果'], symbols: [] },
    { id: 'discounted-return', rule: '加入时间距离，让更近的结果对当前步骤权重更大', latex: String.raw`G_t=\sum_{k=t}^{T}\gamma^{k-t}R_k`, short: '越靠近终局的步骤获得更强信号，但这只是时间权重。', detail: '折扣没有比较替代动作，不能证明较近的 test 或 repair 比早期定位更有因果责任。', assumptions: [String.raw`0\le\gamma\le1`], symbols: [[String.raw`\gamma`, '时间折扣']] },
    { id: 'process-reward', rule: '加入可验证的局部进展，使轨迹内部出现新证据', latex: String.raw`G_t^{\rm process}=\sum_{k=t}^{T}\gamma^{k-t}(R_k^{\rm outcome}+\eta R_k^{\rm process})`, short: '找到正确文件、减少失败测试等证据让信用更稠密。', detail: 'η 表示对过程判据的信任；若 verifier 奖励表面进展，偏差也会被更快写进策略。', assumptions: [String.raw`\eta\ge0`], symbols: [[String.raw`R_k^{\rm process}`, '步骤级过程奖励']] },
    { id: 'segment-advantage', rule: '在工具边界比较实际动作与该状态的价值基线', latex: String.raw`\widehat A_j=\widehat Q(S_{t_j},A_{t_j})-\widehat V(S_{t_j})`, short: 'Search、edit、test 等片段各自获得工具级信用。', detail: '分段减少 token 级噪声，但需要稳定的决策边界和足够数据估计 Q 与 V。', assumptions: ['关键工具边界可识别'], symbols: [[String.raw`t_j`, '第 j 个工具决策边界']] },
    { id: 'hindsight-credit', rule: '从同一状态重放替代动作，比较两条后续结果', latex: String.raw`\begin{aligned}C_t^{\rm hind}&=\widehat Q_{\rm post}(S_t,A_t,\tau_{>t})\\ &\quad-\widehat Q_{\rm post}(S_t,\widetilde A_t,\tau_{>t}).\end{aligned}`, short: '实际 edit 与替代 edit 的结果差提供更直接的选择证据。', detail: '若替代路径只是 critic 在看到答案后编写的故事，仍可能事后合理化；需要环境重放或独立 verifier 审计。', assumptions: ['可构造并重放合理替代动作'], symbols: [[String.raw`\widetilde A_t`, '同状态下的替代动作']] },
  ],
  deepening: [
    { id: 'solution-toolbox', kicker: '从数据到更新的完整流程', title: '长程训练分别处理成功样本不足、信用稀疏和归因偏差', paragraphs: ['训练先规定工具级边界、终局任务判据和可重放日志，再从较短或成功率较高的任务收集正负轨迹。过程 verifier 提供局部证据，segment value 在关键工具边界形成 baseline，高信用步骤则用替代动作重放或独立检查审计。', '这些机制不能压成一个分数：curriculum 改善探索覆盖，过程奖励增加反馈密度，value/critic 降低估计方差，反事实重放提高动作选择性。只有同时保留来源，策略更新后的变化才可解释。'], formulas: [String.raw`\text{long-horizon learning}=\text{exploration}+\text{credit}+\text{reliable feedback}`], pseudocode: ['定义工具级决策边界、终局任务判据和重放条件', '从短 horizon 或较高成功率任务建立初始策略', '收集成功与失败轨迹并保存局部执行证据', '分别计算 outcome、process 与 segment-level signals', '用真实替代动作重放或独立检查审计高信用步骤', '更新策略，并在证据充分时逐步提高任务长度与难度'], handoff: '接下来用同一条轨迹比较五种信号，检查它们是否把信用放到不同步骤。' },
    { id: 'bias-audit', kicker: '更细的信号也带来更多偏差入口', title: '方差下降并不能证明信用更接近真实因果贡献', paragraphs: ['过程模型可能偏爱冗长解释，segment value 可能高估熟悉工具，事后 critic 还可能在看到测试结果后为早期动作虚构理由。更稠密的信号会减少“所有步骤共享一个结果”的随机性，却也可能更稳定地训练错误判断。', '审计应让多种归因方法读取同一条轨迹，找出它们共同认为关键和彼此冲突的步骤；对最高信用的动作，再从保存的状态执行真实重试或替代动作重放。只有结果随动作改变，归因才获得比模型解释更强的证据。'], formulas: [String.raw`\operatorname{Var}(\widehat A)\downarrow\ \not\Rightarrow\ \operatorname{Bias}(\widehat A)\downarrow`], handoff: '不过，归因成立之前还需要足够的成功和失败轨迹可供比较。' },
  ],
  sections: [
    { id: 'curriculum', kicker: '信用需要可比较的数据', title: '初始策略若从不成功，再精细的归因也没有正向轨迹可学', paragraphs: ['在很长的仓库任务上，初始策略可能几乎总是失败；此时 segment value 和反事实比较都缺少足够的成功分支。先从步骤较少、验证清楚的任务开始，再逐步增加工具、环境噪声和 horizon，可以提高正向轨迹的覆盖。', '这种 curriculum 解决的是探索和数据可得性，不会自动指出长轨迹中的关键步骤。任务变长以后，仍要用过程证据、分段 baseline 或可重放替代动作完成归因。'], formula: String.raw`H_1<H_2<\cdots<H_n` },
    { id: 'segmentation', kicker: '先确定归因单位', title: '工具完成时产生的新观察为信用提供稳定边界', paragraphs: ['一次 search、edit 或 test 内部可能包含许多生成 token，但外部环境只在工具执行完成后产生新的文件内容、错误栈或测试结果。若把同一工具调用内部的 token 分别当作独立因果选择，信用会被分散到并未单独改变环境的片段。', '因此先把 search→inspect→edit→test→repair 切成工具级 segment，每个片段从一个可见状态开始，以结构化动作触发环境，再在新 observation 处结束。后面的分段优势才能比较真正改变环境的决策。'], formula: String.raw`\tau=\tau_1\oplus\cdots\oplus\tau_M` },
    { id: 'hindsight-risk', kicker: '实验之后仍需保留的限制', title: '替代动作只有从可比状态真实执行，才构成反事实证据', paragraphs: ['事后 critic 已经看见完整测试结果，容易把后续信息投射回早期搜索或修改，并生成一段听起来合理的解释。实验里显示的 ΔR 只有在 actual 与 alternative 从同一保存状态出发、使用相同预算和 verifier 时，才接近动作的边际效果。', '无法重放时，critic 输出仍可作为候选信用，但必须限制它能读取的信息，并交给独立 verifier 或后续重试审计。长程信用分配最终不是寻找一个万能标量，而是保存足够证据，让每一项归因都能被追溯和反驳。'], formula: String.raw`I_{\rm critic}(t)\subseteq I_{\rm audit}` },
  ], summaryTitle: '长程信用分配是在稀疏结果、稠密代理与反事实偏差间权衡', summary: ['终局广播混淆轨迹中的关键与无关步骤。', '折扣只表达时间距离，不直接证明因果。', '过程奖励和分段价值增加密度但引入模型偏差。', 'Hindsight 信用需要反事实重放或独立验证。'],
}, {
  prerequisite: 'Prerequisites: multi-turn trajectories, returns, TD, and advantage', eyebrow: 'Long-horizon credit assignment', title: 'Terminal outcomes must return to the decisions that changed the task', intro: 'Chapter 20 defined search, inspect, edit, test, and repair as explicit environment steps, yet a repository task may still yield only terminal success or failure. Broadcasting success across the trajectory reinforces detours as much as decisive edits; a final failure can punish a correct early diagnosis undone later. Credit assignment asks how much one decision changed future success relative to plausible alternatives.', bridge: 'Discount adds temporal distance but not causality. Process reward adds local checks but inherits verifier bias. Segment value compares tool decisions with a state baseline. Hindsight counterfactuals require an alternative action replayed from the same state. Finer credit therefore depends on progressively stronger evidence assumptions.', derivationIntro: 'On one tool trajectory, add time, local verification, segment baselines, and alternative outcomes in sequence. Track what each signal distinguishes and what it cannot prove.', experimentIntro: 'Switch among terminal broadcast, discounted return, process reward, segment advantage, and counterfactual replay on one search→inspect→edit→test→repair trajectory. Predict the highest-credit step, then inspect whether the segment ledger and same-state alternative replay support it.', interpretation: 'Denser signal is not necessarily truer attribution. Process verifiers can be wrong, segment baselines miscalibrated, and hindsight critics informed by evidence unavailable at decision time. Outcome differences become stronger causal evidence only when alternatives are replayable from comparable state.', figure: 'Interactive Figure 21.1 · Credit Assignment Microscope', instruction: 'Compare five credit schemes and inspect segment baselines and alternative-action replay', question: 'What evidence is sufficient to show that an early decision changed the terminal outcome?',
  prelude: [
    { id: 'sparse-delay', kicker: 'One outcome covers different roles', title: 'Terminal broadcast compresses decisive edits, recovery, and detours into one signal', paragraphs: ['Consider a successful trajectory that finds the correct file, performs an irrelevant read, edits, tests, and repairs. One success reward cannot identify which steps deserve repetition. A failed trajectory can likewise contain a correct prefix later undone.', 'Terminal outcome defines the end-to-end task, but it provides no within-trajectory comparison. Longer horizons inject more unrelated later choices and environment randomness into every step update, increasing variance and ambiguity.'], formulas: [String.raw`R_0=\cdots=R_{T-1}=0,\qquad R_T\in\{0,1\}`] },
    { id: 'credit-question', kicker: 'The required comparison', title: 'Action credit is the long-term change relative to feasible alternatives', paragraphs: ['At an edit decision, the useful comparison is not merely whether success followed. Hold the visible state fixed and compare the chosen patch with another plausible patch. Advantage expresses this as Q(S_t,A_t) minus the value normally achievable from that state.', 'Repositories rarely permit exhaustive replay from every hidden state, so process rewards, segment values, and hindsight critics approximate an incomplete comparison. Each must state where new evidence comes from, which quantities are estimated, and how future-information leakage is audited.'], formulas: [String.raw`A(S_t,A_t)=Q(S_t,A_t)-V(S_t)`] },
  ],
  derivation: [
    { id: 'terminal-broadcast', rule: 'Broadcast terminal outcome to every step', latex: String.raw`\widehat A_t^{\rm terminal}=R(\tau),\qquad t=0,\ldots,T`, short: 'Simple, but every action receives one direction.', detail: 'It cannot punish detours in success or protect good prefixes in failure.', assumptions: ['All steps share responsibility'], symbols: [] },
    { id: 'discounted-return', rule: 'Discount the influence of distant outcomes', latex: String.raw`G_t=\sum_{k=t}^{T}\gamma^{k-t}R_k`, short: 'Steps closer to outcome receive stronger signal.', detail: 'Temporal proximity is not proof of causal responsibility.', assumptions: [String.raw`0\le\gamma\le1`], symbols: [[String.raw`\gamma`, 'temporal discount']] },
    { id: 'process-reward', rule: 'Add process reward for checkable milestones', latex: String.raw`G_t^{\rm process}=\sum_{k=t}^{T}\gamma^{k-t}(R_k^{\rm outcome}+\eta R_k^{\rm process})`, short: 'Test improvement and correct localization add dense evidence.', detail: 'η expresses trust in process criteria; wrong process reward systematically trains the wrong policy.', assumptions: [String.raw`\eta\ge0`], symbols: [[String.raw`R_k^{\rm process}`, 'step-level process reward']] },
    { id: 'segment-advantage', rule: 'Estimate value difference at key decision boundaries', latex: String.raw`\widehat A_j=\widehat Q(S_{t_j},A_{t_j})-\widehat V(S_{t_j})`, short: 'Credit at tool or subgoal boundaries reduces token noise.', detail: 'Segmentation requires stable boundaries and value data.', assumptions: ['Key decisions are identifiable'], symbols: [[String.raw`t_j`, 'decision boundary j']] },
    { id: 'hindsight-credit', rule: 'After observing the result, estimate counterfactual necessity', latex: String.raw`\begin{aligned}C_t^{\rm hind}&=\widehat Q_{\rm post}(S_t,A_t,\tau_{>t})\\ &\quad-\widehat Q_{\rm post}(S_t,\widetilde A_t,\tau_{>t}).\end{aligned}`, short: 'A post-hoc critic compares actual and plausible alternative actions.', detail: 'It uses complete evidence but can rationalize; replay or an independent verifier must audit it.', assumptions: ['A plausible alternative action can be constructed'], symbols: [[String.raw`\widetilde A_t`, 'counterfactual action']] },
  ],
  deepening: [
    { id: 'solution-toolbox', kicker: 'Complete path from data to update', title: 'Long-horizon training handles missing success data, sparse credit, and attribution bias separately', paragraphs: ['Define tool boundaries, terminal criteria, and replayable logs before collecting shorter success and failure trajectories. Process verifiers add local evidence; segment values establish baselines at tool decisions; replay or independent checks audit high-credit actions.', 'Curriculum improves exploration, process reward increases density, value or critic reduces variance, and counterfactual replay increases selectivity. Keeping their evidence sources separate makes policy changes explainable.'], formulas: [String.raw`\text{long-horizon learning}=\text{exploration}+\text{credit}+\text{reliable feedback}`], pseudocode: ['Define tool-level boundaries, terminal criteria, and replay conditions', 'Start with short or higher-success tasks', 'Collect success and failure trajectories with local evidence', 'Compute outcome, process, and segment-level signals separately', 'Audit high-credit actions with real alternative replay or independent checks', 'Update policy and increase horizon only when evidence supports it'], handoff: 'The experiment now compares five signals on the same trajectory.' },
    { id: 'bias-audit', kicker: 'Finer signals add more entry points for bias', title: 'Lower variance does not prove that credit is closer to causal contribution', paragraphs: ['A process model may prefer verbose reasoning, a segment value model familiar tools, and a hindsight critic a story invented after seeing test results. Dense signal reduces randomness from one shared outcome but can train the wrong judgment more consistently.', 'Run attribution methods on the same trajectory, locate agreement and conflict, then replay the highest-credit alternatives from saved state. A changed outcome is stronger evidence than a model explanation.'], formulas: [String.raw`\operatorname{Var}(\widehat A)\downarrow\ \not\Rightarrow\ \operatorname{Bias}(\widehat A)\downarrow`], handoff: 'Attribution still needs enough successful and failed trajectories to compare.' },
  ],
  sections: [
    { id: 'curriculum', kicker: 'Credit requires comparable data', title: 'If the initial policy never succeeds, finer attribution has no positive branch to learn from', paragraphs: ['On long repository tasks, the initial policy may fail almost always, leaving segment values and counterfactual comparison without enough successful paths. Begin with shorter, clearly verified tasks before adding tools, noise, and horizon.', 'Curriculum improves exploration and data availability; it does not identify causal steps. Longer tasks still need process evidence, segment baselines, or replayable alternatives.'], formula: String.raw`H_1<H_2<\cdots<H_n` },
    { id: 'segmentation', kicker: 'Choose the attribution unit first', title: 'New observations at tool completion define stable credit boundaries', paragraphs: ['A search, edit, or test contains many generated tokens, yet the environment emits a new file view, stack trace, or test result only after the tool finishes. Tokenwise attribution would spread credit across fragments that did not independently change the environment.', 'Segment search→inspect→edit→test→repair at tool boundaries: each segment starts from visible state, triggers the environment with one structured action, and ends at a new observation. Segment advantage can then compare decisions that actually changed the task.'], formula: String.raw`\tau=\tau_1\oplus\cdots\oplus\tau_M` },
    { id: 'hindsight-risk', kicker: 'Limitation after the experiment', title: 'An alternative becomes counterfactual evidence only when executed from comparable state', paragraphs: ['A hindsight critic sees full test results and can project later evidence backward into a convincing story. The displayed ΔR is meaningful only when actual and alternative begin from the same saved state, use the same budget, and face the same verifier.', 'Without replay, critic output remains candidate credit that needs bounded information and independent audit. Long-horizon credit is not one universal scalar; it is an evidence record that lets each attribution be traced and challenged.'], formula: String.raw`I_{\rm critic}(t)\subseteq I_{\rm audit}` },
  ], summaryTitle: 'Long-horizon credit balances sparse outcomes, dense proxies, and counterfactual bias', summary: ['Terminal broadcast confuses key and irrelevant steps.', 'Discount is temporal, not automatically causal.', 'Process and segment values add density and model bias.', 'Hindsight credit requires replay or independent verification.'],
})

Object.assign(dpoChapter.zh, { eyebrow: '第 17 章 · DPO 离线偏好优化', title: '从 chosen/rejected 回答直接优化策略', derivationEyebrow: '隐式奖励消元', derivationTitle: '从 KL 正则最优策略得到成对偏好损失' })
Object.assign(dpoChapter.en, { eyebrow: 'Chapter 17 · DPO offline preference optimization', title: 'Direct policy optimization from chosen and rejected responses', derivationEyebrow: 'Eliminating implicit reward', derivationTitle: 'Derive pairwise preference loss from a KL-regularized optimum' })
Object.assign(grpoChapter.zh, { eyebrow: '第 18 章 · GRPO 在线可验证奖励', title: '以同组回答替代 Critic 的在线策略优化', derivationEyebrow: '组相对优势', derivationTitle: '从同组奖励得到裁剪策略目标' })
Object.assign(grpoChapter.en, { eyebrow: 'Chapter 18 · GRPO with online verifiable rewards', title: 'Online policy optimization with response groups instead of a critic', derivationEyebrow: 'Group-relative advantage', derivationTitle: 'Derive a clipped policy objective from within-group rewards' })
Object.assign(codingRlChapter.zh, { eyebrow: '第 19 章 · Coding RL', title: '以编译器和测试反馈构造可验证强化学习', derivationEyebrow: '可执行反馈', derivationTitle: '把执行结果写成训练奖励与下一状态' })
Object.assign(codingRlChapter.en, { eyebrow: 'Chapter 19 · Coding RL', title: 'Verifiable reinforcement learning from compiler and test feedback', derivationEyebrow: 'Executable feedback', derivationTitle: 'Turn execution results into training reward and next state' })
Object.assign(agentMdpChapter.zh, { eyebrow: '第 20 章 · 多轮 Agent MDP', title: '工具调用、环境观察与记忆构成 Agent 轨迹', derivationEyebrow: '工具交互边界', derivationTitle: '从工具调用边界得到 Agent 轨迹概率' })
Object.assign(agentMdpChapter.en, { eyebrow: 'Chapter 20 · Multi-turn Agent MDP', title: 'Agent trajectories composed of tool calls, observations, and memory', derivationEyebrow: 'Tool-interaction boundaries', derivationTitle: 'Derive agent-trajectory probability from tool-call boundaries' })
Object.assign(creditChapter.zh, { eyebrow: '第 21 章 · 长程信用分配', title: '将终局结果归因于长程轨迹中的关键决策', derivationEyebrow: '归因尺度', derivationTitle: '比较终局、过程与反事实信用' })
Object.assign(creditChapter.en, { eyebrow: 'Chapter 21 · Long-horizon credit assignment', title: 'Assigning terminal outcomes to key decisions in long trajectories', derivationEyebrow: 'Attribution scales', derivationTitle: 'Compare terminal, process, and counterfactual credit' })
